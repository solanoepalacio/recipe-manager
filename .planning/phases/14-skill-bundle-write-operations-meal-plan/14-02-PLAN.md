---
phase: 14-skill-bundle-write-operations-meal-plan
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/recipe-manager/recipes_image.md
  - skills/recipe-manager/meal_plan.md
autonomous: true
requirements: [SKILL-07, SKILL-08]

must_haves:
  truths:
    - "Agent reading recipes_image.md can upload a multipart image with the correct field name and MIME types"
    - "Agent reading recipes_image.md can delete an existing image using the recipe id and image id"
    - "Agent reading meal_plan.md can list entries for a date range using from and to query params"
    - "Agent reading meal_plan.md can create an entry with a valid mealType value"
    - "Agent reading meal_plan.md knows the response is a flat entries array, not a paginated envelope"
  artifacts:
    - path: "skills/recipe-manager/recipes_image.md"
      provides: "Multipart image upload and delete documentation"
      contains: "multipart/form-data"
    - path: "skills/recipe-manager/meal_plan.md"
      provides: "Meal plan CRUD documentation with MealType enum values"
      contains: "POST /api/meal-plan/entries"
  key_links:
    - from: "skills/recipe-manager/recipes_image.md"
      to: "skills/recipe-manager/recipes_get.md"
      via: "cross-reference for obtaining recipe id and image ids"
      pattern: "recipes_get\\.md"
    - from: "skills/recipe-manager/meal_plan.md"
      to: "skills/recipe-manager/recipes_search.md"
      via: "cross-reference for obtaining recipeId"
      pattern: "recipes_search\\.md"
    - from: "skills/recipe-manager/meal_plan.md"
      to: "skills/recipe-manager/shared.md"
      via: "cross-reference for auth"
      pattern: "shared\\.md"
---

<objective>
Create the two remaining skill files: recipes_image.md (multipart upload and delete) and meal_plan.md (list, create, update, delete entries).

Purpose: These files complete the skill bundle. recipes_image.md enables an agent to attach and remove images from recipes. meal_plan.md enables an agent to manage the household meal plan.
Output: skills/recipe-manager/recipes_image.md, skills/recipe-manager/meal_plan.md
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-skill-bundle-write-operations-meal-plan/14-RESEARCH.md
@plans/02_Skill/skill-bundle-spec.md
@packages/shared/src/api/meal-plan.ts
@packages/shared/src/enums.ts
</context>

<interfaces>
<!-- Key types the executor needs — extracted from source -->

From packages/shared/src/api/meal-plan.ts:
```typescript
export interface MealPlanEntryResponse {
  id: string;
  date: string;
  mealType: MealType;
  recipeId: string;
  recipeName: string;
  recipeSlug: string;
  createdAt: string;
  updatedAt: string;
}

export interface MealPlanResponse {
  entries: MealPlanEntryResponse[];
}

export interface CreateMealPlanEntryRequest {
  recipeId: string;
  date: string;
  mealType: MealType;
}

export interface UpdateMealPlanEntryRequest {
  recipeId?: string;
  date?: string;
  mealType?: MealType;
}
```

From packages/shared/src/enums.ts:
```typescript
export enum MealType {
  Breakfast = 'breakfast',
  Lunch = 'lunch',
  Dinner = 'dinner',
  Snack = 'snack',
  Dessert = 'dessert',
}
```

From packages/shared/src/api/recipes.ts:
```typescript
export interface ImageResponse {
  id: string;
  url: string;
  order: number;
  createdAt: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create recipes_image.md</name>
  <files>skills/recipe-manager/recipes_image.md</files>
  <read_first>
    - plans/02_Skill/skill-bundle-spec.md (canonical requirements for recipes_image.md)
    - .planning/phases/14-skill-bundle-write-operations-meal-plan/14-RESEARCH.md (verified API contracts — SKILL-07 section)
    - skills/recipe-manager/recipes_get.md (format reference and ImageResponse shape)
  </read_first>
  <action>
Create `skills/recipe-manager/recipes_image.md` covering image upload and delete. This is a simpler file with only 2 endpoints.

**Title:** `# Manage recipe images`

**Section: Upload an image**

H2: `## Upload an image`
H3: `### Endpoint`
```
POST /api/recipes/:id/images
```
Authentication required (see `shared.md`). Uploads a single image file to the recipe.

- `201` — uploaded
- `400` — invalid MIME type or file exceeds size limit
- `403` — recipe belongs to a different household
- `404` — recipe not found

H3: `### Request format`

The request must use `multipart/form-data` encoding with a single field:

| Field | Type | Description |
|-------|------|-------------|
| file | binary | The image file |

Accepted MIME types:

| MIME type |
|-----------|
| `image/jpeg` |
| `image/png` |
| `image/webp` |
| `image/gif` |

Maximum file size: **10 MB**.

H3: `### Response`

```json
{
  "id": "im1im2im3-...",
  "url": "/uploads/a1b2c3d4.jpg",
  "order": 0,
  "createdAt": "2026-03-20T12:00:00.000Z"
}
```

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | string (UUID) | No | Image ID — use for delete |
| url | string | No | Image URL path relative to base URL (e.g., `/uploads/a1b2c3d4.jpg`) |
| order | number | No | Display order (0-based) |
| createdAt | string (ISO 8601) | No | Upload timestamp |

H3: `### Example`

Request (pseudo-HTTP):
```
POST /api/recipes/r1r2r3r4-.../images HTTP/1.1
Authorization: Bearer <token>
Content-Type: multipart/form-data; boundary=----boundary

------boundary
Content-Disposition: form-data; name="file"; filename="tortilla.jpg"
Content-Type: image/jpeg

<binary data>
------boundary--
```

Response:
```json
{
  "id": "im1im2im3-...",
  "url": "/uploads/a1b2c3d4.jpg",
  "order": 0,
  "createdAt": "2026-03-20T12:00:00.000Z"
}
```

**Section: Delete an image**

H2: `## Delete an image`
H3: `### Endpoint`
```
DELETE /api/recipes/:id/images/:imageId
```
- `200` — deleted
- `403` — recipe belongs to a different household
- `404` — recipe or image not found

No request body is needed. Obtain the `imageId` from `GET /api/recipes/:id` (see `recipes_get.md`) — it is the `id` field in the `images[]` array.

**Section: Cross-references**

H2: `## Cross-references`

- To obtain the recipe `id` and existing image IDs: see `recipes_get.md`.
- To create the recipe before uploading images: see `recipes_create.md`.
- Authentication and error codes: see `shared.md`.
  </action>
  <verify>
    <automated>grep "POST /api/recipes/:id/images" skills/recipe-manager/recipes_image.md && grep "multipart" skills/recipe-manager/recipes_image.md && grep "DELETE /api/recipes/:id/images/:imageId" skills/recipe-manager/recipes_image.md && grep "image/jpeg" skills/recipe-manager/recipes_image.md && grep "image/png" skills/recipe-manager/recipes_image.md && grep "image/webp" skills/recipe-manager/recipes_image.md && grep "10 MB" skills/recipe-manager/recipes_image.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/recipe-manager/recipes_image.md` exists
    - Contains `POST /api/recipes/:id/images` (upload endpoint)
    - Contains `DELETE /api/recipes/:id/images/:imageId` (delete endpoint)
    - Contains `multipart/form-data` (request encoding)
    - Contains field name `file` for the upload field
    - Contains all 4 MIME types: `image/jpeg`, `image/png`, `image/webp`, `image/gif`
    - Contains `10 MB` (max file size)
    - Contains response field table with id, url, order, createdAt
    - Contains pseudo-HTTP multipart example with boundary
    - Contains cross-reference to `recipes_get.md`, `recipes_create.md`, `shared.md`
  </acceptance_criteria>
  <done>recipes_image.md documents multipart upload (field name, MIME types, 10MB limit) with pseudo-HTTP example, delete endpoint, response shape, and cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Create meal_plan.md</name>
  <files>skills/recipe-manager/meal_plan.md</files>
  <read_first>
    - plans/02_Skill/skill-bundle-spec.md (canonical requirements for meal_plan.md)
    - .planning/phases/14-skill-bundle-write-operations-meal-plan/14-RESEARCH.md (verified API contracts — SKILL-08 section)
    - skills/recipe-manager/recipes_search.md (format reference for field tables)
    - packages/shared/src/api/meal-plan.ts (MealPlanResponse, MealPlanEntryResponse, CreateMealPlanEntryRequest, UpdateMealPlanEntryRequest)
    - packages/shared/src/enums.ts (MealType enum values)
  </read_first>
  <action>
Create `skills/recipe-manager/meal_plan.md` covering all 4 meal plan operations. This is a multi-endpoint file.

**Title:** `# Manage meal plan`

**Important note** (immediately after title):
The meal plan list endpoint returns a flat `entries` array, **not** a paginated envelope. Do not look for `items`, `total`, `page`, or `perPage` in the response.

**Section: MealType values**

H2: `## MealType values`

All endpoints that accept or return a `mealType` field use one of these values:

| Value | Description |
|-------|-------------|
| `breakfast` | Breakfast |
| `lunch` | Lunch |
| `dinner` | Dinner |
| `snack` | Snack |
| `dessert` | Dessert |

**Section: List entries**

H2: `## List entries`
H3: `### Endpoint`
```
GET /api/meal-plan
```
Authentication required (see `shared.md`). Returns all meal plan entries for the authenticated user's household, optionally filtered by date range.

- `200` — success

H3: `### Query parameters`

| Param | Type | Required | Description |
|-------|------|----------|-------------|
| from | string (YYYY-MM-DD) | No | Start date (inclusive). Omit for no lower bound. |
| to | string (YYYY-MM-DD) | No | End date (inclusive). Omit for no upper bound. |

Common queries:
- Today's entries: `GET /api/meal-plan?from=2026-03-20&to=2026-03-20`
- This week (Mon-Sun): `GET /api/meal-plan?from=2026-03-16&to=2026-03-22`
- All entries: `GET /api/meal-plan` (no params)

H3: `### Response`

The response is a `MealPlanResponse` object with an `entries` array:

```json
{
  "entries": [
    {
      "id": "e1e2e3e4-...",
      "date": "2026-03-20",
      "mealType": "lunch",
      "recipeId": "r1r2r3r4-...",
      "recipeName": "Tortilla espanola",
      "recipeSlug": "tortilla-espanola",
      "createdAt": "2026-03-19T10:00:00.000Z",
      "updatedAt": "2026-03-19T10:00:00.000Z"
    }
  ]
}
```

Entry field types:

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | string (UUID) | No | Entry ID — use for update and delete |
| date | string (YYYY-MM-DD) | No | Assigned date |
| mealType | string | No | One of the MealType values above |
| recipeId | string (UUID) | No | Recipe ID |
| recipeName | string | No | Recipe name (denormalized) |
| recipeSlug | string | No | Recipe slug (denormalized) |
| createdAt | string (ISO 8601) | No | Creation timestamp |
| updatedAt | string (ISO 8601) | No | Last update timestamp |

**Section: Create an entry**

H2: `## Create an entry`
H3: `### Endpoint`
```
POST /api/meal-plan/entries
```
- `201` — created
- `400` — validation error

H3: `### Request body`

All three fields are required:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| recipeId | string (UUID) | Yes | Recipe ID to assign (obtain from `recipes_search.md`) |
| date | string | Yes | Date in `YYYY-MM-DD` format |
| mealType | string | Yes | One of: `breakfast`, `lunch`, `dinner`, `snack`, `dessert` |

H3: `### Response`
`MealPlanEntryResponse` (same shape as entries in the list response above).

H3: `### Example`

Request:
```
POST /api/meal-plan/entries
```
Request body:
```json
{
  "recipeId": "r1r2r3r4-...",
  "date": "2026-03-21",
  "mealType": "dinner"
}
```
Response:
```json
{
  "id": "e5e6e7e8-...",
  "date": "2026-03-21",
  "mealType": "dinner",
  "recipeId": "r1r2r3r4-...",
  "recipeName": "Tortilla espanola",
  "recipeSlug": "tortilla-espanola",
  "createdAt": "2026-03-20T15:00:00.000Z",
  "updatedAt": "2026-03-20T15:00:00.000Z"
}
```

**Section: Update an entry**

H2: `## Update an entry`
H3: `### Endpoint`
```
PATCH /api/meal-plan/entries/:id
```
Partial update — include only the fields you want to change.

- `200` — updated
- `403` — entry belongs to a different household
- `404` — entry not found

H3: `### Request body`

All fields are optional:

| Field | Type | Description |
|-------|------|-------------|
| recipeId | string (UUID) | Change the assigned recipe |
| date | string | Change the date (`YYYY-MM-DD`) |
| mealType | string | Change the meal type |

H3: `### Response`
`MealPlanEntryResponse`.

**Section: Delete an entry**

H2: `## Delete an entry`
H3: `### Endpoint`
```
DELETE /api/meal-plan/entries/:id
```
- `200` — deleted
- `403` — entry belongs to a different household
- `404` — entry not found

No request body is needed. Obtain the entry `id` from `GET /api/meal-plan` list response.

**Section: Cross-references**

H2: `## Cross-references`

- To obtain a `recipeId` for creating entries: search recipes via `GET /api/recipes` (see `recipes_search.md`).
- Authentication and error codes: see `shared.md`.
  </action>
  <verify>
    <automated>grep "GET /api/meal-plan" skills/recipe-manager/meal_plan.md && grep "POST /api/meal-plan/entries" skills/recipe-manager/meal_plan.md && grep "PATCH /api/meal-plan/entries/:id" skills/recipe-manager/meal_plan.md && grep "DELETE /api/meal-plan/entries/:id" skills/recipe-manager/meal_plan.md && grep "breakfast" skills/recipe-manager/meal_plan.md && grep "lunch" skills/recipe-manager/meal_plan.md && grep "dinner" skills/recipe-manager/meal_plan.md && grep "snack" skills/recipe-manager/meal_plan.md && grep "dessert" skills/recipe-manager/meal_plan.md && grep "entries" skills/recipe-manager/meal_plan.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/recipe-manager/meal_plan.md` exists
    - Contains `GET /api/meal-plan` (list endpoint)
    - Contains `POST /api/meal-plan/entries` (create endpoint)
    - Contains `PATCH /api/meal-plan/entries/:id` (update endpoint)
    - Contains `DELETE /api/meal-plan/entries/:id` (delete endpoint)
    - Contains all 5 MealType values: `breakfast`, `lunch`, `dinner`, `snack`, `dessert`
    - Contains `entries` in the response JSON (flat array, not paginated)
    - Does NOT contain `"items"` in any meal plan response JSON block (it uses `entries`, not `items`)
    - Does NOT contain `"perPage"` in any meal plan response JSON block (not paginated)
    - Contains `from` and `to` query parameters with `YYYY-MM-DD` format
    - Contains all 8 entry response fields: id, date, mealType, recipeId, recipeName, recipeSlug, createdAt, updatedAt
    - Contains at least one full request/response example for creating an entry
    - Contains cross-reference to `recipes_search.md` and `shared.md`
  </acceptance_criteria>
  <done>meal_plan.md documents all 4 operations (list with date range, create, update, delete), all 5 MealType enum values, flat entries response (not paginated), and worked example for entry creation</done>
</task>

</tasks>

<verification>
1. `ls skills/recipe-manager/` shows recipes_image.md and meal_plan.md alongside existing files
2. recipes_image.md documents multipart upload with correct field name, MIME types, and 10MB limit
3. recipes_image.md documents DELETE with imageId path parameter
4. meal_plan.md documents all 4 CRUD operations with correct endpoint paths
5. meal_plan.md lists all 5 MealType enum values
6. meal_plan.md response uses `entries` array (NOT `items` paginated envelope)
7. Both files follow the same heading/table/example style as existing skill files
</verification>

<success_criteria>
- An agent reading recipes_image.md can upload a multipart image with the correct field name and MIME types and delete an existing image
- An agent reading meal_plan.md can list entries for a date range, create an entry with a valid mealType, update an entry, and delete an entry — all without consulting any other source
</success_criteria>

<output>
After completion, create `.planning/phases/14-skill-bundle-write-operations-meal-plan/14-02-SUMMARY.md`
</output>

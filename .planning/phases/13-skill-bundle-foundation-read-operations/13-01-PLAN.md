---
phase: 13-skill-bundle-foundation-read-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/recipe-manager/index.md
  - skills/recipe-manager/shared.md
autonomous: true
requirements: [SKILL-01, SKILL-02]

must_haves:
  truths:
    - "Agent reading only index.md knows which file to open for any recipe-manager operation"
    - "Agent reading shared.md can construct a correctly authenticated request with Bearer token"
    - "Agent reading shared.md can interpret any error status code (400, 401, 403, 404, 500)"
    - "Agent reading shared.md can parse a paginated response using the correct field names (items, total, page, perPage)"
  artifacts:
    - path: "skills/recipe-manager/index.md"
      provides: "Startup index listing all 8 skill files with one-line descriptions"
      contains: "shared.md"
    - path: "skills/recipe-manager/shared.md"
      provides: "Auth, error codes, and pagination envelope documentation"
      contains: "Authorization: Bearer"
  key_links:
    - from: "skills/recipe-manager/index.md"
      to: "skills/recipe-manager/shared.md"
      via: "cross-reference in index"
      pattern: "shared\\.md"
---

<objective>
Create the skill bundle directory and the two foundation files: index.md (startup index) and shared.md (auth, errors, pagination).

Purpose: These two files form the foundation every agent reads before performing any operation. index.md is the entry point; shared.md covers auth and conventions needed by all other skill files.
Output: skills/recipe-manager/index.md, skills/recipe-manager/shared.md
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@plans/02_Skill/skill-bundle-spec.md
@packages/shared/src/common.ts
@apps/api/src/auth/guards/api-key.guard.ts
@apps/api/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skills directory and index.md</name>
  <files>skills/recipe-manager/index.md</files>
  <read_first>
    - plans/02_Skill/skill-bundle-spec.md (canonical requirements for index.md content)
  </read_first>
  <action>
Create the directory `skills/recipe-manager/` at the monorepo root.

Create `skills/recipe-manager/index.md` with the following exact content structure:

1. A title: `# recipe-manager skill bundle`
2. A one-sentence purpose statement: this bundle teaches an agent to interact with the recipe-manager REST API.
3. A "Read `shared.md` first" instruction: before any operation, read `shared.md` to set up authentication and understand error handling.
4. A "Files" section as a Markdown table with two columns: `File` and `Description`. List all 8 files:

| File | Description |
|------|-------------|
| `shared.md` | Authentication, base URL, error codes, pagination envelope |
| `recipes_search.md` | Search and list recipes with filters, sorting, and pagination |
| `recipes_get.md` | Fetch full recipe detail including sections, ingredients, steps, and images |
| `recipes_create.md` | Create a recipe with sections, ingredients, and steps |
| `recipes_edit.md` | Update or delete recipe metadata, sections, ingredients, and steps |
| `recipes_image.md` | Upload and delete recipe images |
| `meal_plan.md` | List, create, update, and delete meal plan entries |

5. A "Usage" instruction: read the relevant file before each operation class. Do not load all files at once.

CRITICAL: index.md must NOT contain any endpoint paths, HTTP methods, query parameters, or request/response shapes. It only lists files and descriptions.
  </action>
  <verify>
    <automated>test -d skills/recipe-manager && grep -c "shared.md" skills/recipe-manager/index.md && grep -c "recipes_search.md" skills/recipe-manager/index.md && grep -c "recipes_get.md" skills/recipe-manager/index.md && grep -c "recipes_create.md" skills/recipe-manager/index.md && grep -c "recipes_edit.md" skills/recipe-manager/index.md && grep -c "recipes_image.md" skills/recipe-manager/index.md && grep -c "meal_plan.md" skills/recipe-manager/index.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/recipe-manager/` directory exists
    - `skills/recipe-manager/index.md` exists and contains all 8 file names: shared.md, recipes_search.md, recipes_get.md, recipes_create.md, recipes_edit.md, recipes_image.md, meal_plan.md
    - index.md does NOT contain any of: `GET `, `POST `, `PATCH `, `DELETE `, `/api/`
    - index.md contains the string "shared.md" at least twice (once in table, once in read-first instruction)
  </acceptance_criteria>
  <done>index.md lists all 8 bundle files with one-line descriptions; contains no endpoint documentation; instructs agent to read shared.md first</done>
</task>

<task type="auto">
  <name>Task 2: Create shared.md</name>
  <files>skills/recipe-manager/shared.md</files>
  <read_first>
    - plans/02_Skill/skill-bundle-spec.md (canonical requirements for shared.md content)
    - packages/shared/src/common.ts (PaginatedResponse and ErrorResponse shapes — source of truth)
    - apps/api/src/auth/guards/api-key.guard.ts (Bearer auth mechanism)
    - apps/api/src/main.ts (global prefix /api)
  </read_first>
  <action>
Create `skills/recipe-manager/shared.md` with these exact sections:

**Title:** `# Shared conventions`

**Section 1: Base URL**
- All endpoint paths in this bundle are relative to a configurable base URL.
- Default base URL: `http://localhost:3001` (the NestJS API server).
- All paths start with `/api` (e.g., `GET /api/recipes`).

**Section 2: Authentication**
- Every request must include the header: `Authorization: Bearer <token>`
- The token is a raw API key issued by an administrator. Do not hash or encode it.
- Example:
```
GET /api/recipes HTTP/1.1
Authorization: Bearer abc123def456...
```

**Section 3: Error codes**
A Markdown table with these exact rows:

| Status | Meaning | Action |
|--------|---------|--------|
| 400 | Validation error — request body or query params are invalid | Check the `message` field (array of strings) for details |
| 401 | Not authenticated — token is missing or invalid | Verify the `Authorization` header is present and the token is correct |
| 403 | Forbidden — authenticated but not authorized for this resource | The resource belongs to a different household |
| 404 | Not found — the resource does not exist | Verify the ID or path is correct |
| 422 | Validation error (alternative) — same as 400 | Check the `message` field |
| 500 | Server error | Do not retry immediately; report the issue |

**Section 4: Error response shape**
```json
{
  "statusCode": 404,
  "message": "Recipe abc123 not found",
  "error": "Not Found"
}
```
Note: for validation errors (400), `message` is an array of strings:
```json
{
  "statusCode": 400,
  "message": ["name must be a string", "servingsQty must be a number"],
  "error": "Bad Request"
}
```

**Section 5: Pagination**
List endpoints return a paginated envelope:
```json
{
  "items": [],
  "total": 100,
  "page": 1,
  "perPage": 20
}
```

Field descriptions as a table:

| Field | Type | Description |
|-------|------|-------------|
| items | array | The page of results |
| total | integer | Total number of matching items across all pages |
| page | integer | Current 1-based page number |
| perPage | integer | Number of items per page |

IMPORTANT: The response field is `perPage` (not `pageSize`). The query parameter for requesting page size is `pageSize`. These are different names — do not confuse them.
  </action>
  <verify>
    <automated>grep "Authorization: Bearer" skills/recipe-manager/shared.md && grep "perPage" skills/recipe-manager/shared.md && grep "400" skills/recipe-manager/shared.md && grep "401" skills/recipe-manager/shared.md && grep "403" skills/recipe-manager/shared.md && grep "404" skills/recipe-manager/shared.md && grep "500" skills/recipe-manager/shared.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/recipe-manager/shared.md` exists
    - Contains `Authorization: Bearer` (auth header format)
    - Contains `/api` (base URL convention)
    - Contains all 5 error status codes: 400, 401, 403, 404, 500
    - Contains `perPage` in the pagination envelope (NOT `pageSize` in response)
    - Contains `"items"` in the pagination JSON block
    - Contains `"total"` in the pagination JSON block
    - Contains the word `pageSize` at least once (to document the query param vs response field distinction)
  </acceptance_criteria>
  <done>shared.md documents Bearer auth, base URL /api, all error codes with meanings, error response shape (including validation array), and pagination envelope with correct field names (perPage in response, pageSize in query)</done>
</task>

</tasks>

<verification>
1. `ls skills/recipe-manager/` shows index.md and shared.md
2. index.md references all 8 bundle files
3. index.md contains zero endpoint paths (no `/api/` substring)
4. shared.md documents Bearer auth, 5 error codes, pagination with `perPage`
</verification>

<success_criteria>
- An agent reading only index.md can determine which file to read for any recipe-manager operation
- An agent reading shared.md can construct an authenticated request with the correct header format
- An agent reading shared.md can interpret any error response and parse any paginated response
</success_criteria>

<output>
After completion, create `.planning/phases/13-skill-bundle-foundation-read-operations/13-01-SUMMARY.md`
</output>

---
phase: 13-skill-bundle-foundation-read-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/recipe-manager/recipes_search.md
  - skills/recipe-manager/recipes_get.md
autonomous: true
requirements: [SKILL-03, SKILL-04]

must_haves:
  truths:
    - "Agent reading recipes_search.md can call GET /api/recipes with any combination of search, foodId, sort, order, page, and pageSize parameters"
    - "Agent reading recipes_search.md can parse the paginated response and extract recipe list items"
    - "Agent reading recipes_get.md can call GET /api/recipes/:id and extract sections, ingredients, steps, and images from the response"
    - "Agent reading recipes_get.md knows which fields are nullable"
  artifacts:
    - path: "skills/recipe-manager/recipes_search.md"
      provides: "GET /api/recipes endpoint documentation with all query params and response shape"
      contains: "GET /api/recipes"
    - path: "skills/recipe-manager/recipes_get.md"
      provides: "GET /api/recipes/:id endpoint documentation with full nested response shape"
      contains: "GET /api/recipes/:id"
  key_links:
    - from: "skills/recipe-manager/recipes_search.md"
      to: "skills/recipe-manager/shared.md"
      via: "cross-reference for pagination and auth"
      pattern: "shared\\.md"
    - from: "skills/recipe-manager/recipes_get.md"
      to: "skills/recipe-manager/recipes_search.md"
      via: "cross-reference for obtaining recipe id"
      pattern: "recipes_search\\.md"
---

<objective>
Create the two read-operation skill files: recipes_search.md (search/list recipes) and recipes_get.md (fetch full recipe detail).

Purpose: These files enable an agent to perform all read operations on recipes. recipes_search.md covers listing with filters; recipes_get.md covers fetching the full detail with all nested objects.
Output: skills/recipe-manager/recipes_search.md, skills/recipe-manager/recipes_get.md
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@plans/02_Skill/skill-bundle-spec.md
@packages/shared/src/api/recipes.ts
@packages/shared/src/common.ts
@apps/api/src/recipes/dto/recipe-query.dto.ts
</context>

<interfaces>
<!-- Key types the executor needs — extracted from packages/shared/src/api/recipes.ts -->

```typescript
export interface RecipeListItem {
  id: string;
  name: string;
  slug: string;
  description: string | null;
  servingsQty: number | null;
  servingsUnit: string | null;
  shareToken: string | null;
  createdAt: string;
  updatedAt: string;
  imageCount: number;
  coverImageUrl: string | null;
}

export interface RecipeQueryParams {
  search?: string;
  foodId?: string;
  sort?: 'name' | 'createdAt' | 'updatedAt' | 'random';
  order?: 'asc' | 'desc';
  page?: number;
  pageSize?: number;
}

export interface RecipeDetailResponse {
  id: string;
  householdId: string;
  createdById: string;
  name: string;
  slug: string;
  description: string | null;
  servingsQty: number | null;
  servingsUnit: string | null;
  prepTime: number | null;
  cookTime: number | null;
  totalTime: number | null;
  performTime: number | null;
  sourceUrl: string | null;
  isLocked: boolean;
  shareToken: string | null;
  createdAt: string;
  updatedAt: string;
  sections: SectionResponse[];
  steps: StepResponse[];
  images: ImageResponse[];
}

export interface SectionResponse {
  id: string;
  title: string | null;
  order: number;
  ingredients: IngredientResponse[];
}

export interface IngredientResponse {
  id: string;
  foodId: string;
  foodName: string;
  unitId: string | null;
  unitName: string | null;
  quantity: number | null;
  note: string | null;
  order: number;
}

export interface StepResponse {
  id: string;
  title: string | null;
  body: string;
  order: number;
}

export interface ImageResponse {
  id: string;
  url: string;
  order: number;
  createdAt: string;
}
```

From packages/shared/src/common.ts:
```typescript
export interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  perPage: number;
}

export interface ErrorResponse {
  statusCode: number;
  message: string | string[];
  error?: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create recipes_search.md</name>
  <files>skills/recipe-manager/recipes_search.md</files>
  <read_first>
    - plans/02_Skill/skill-bundle-spec.md (canonical requirements for recipes_search.md)
    - packages/shared/src/api/recipes.ts (RecipeListItem and RecipeQueryParams — source of truth for field names and types)
    - packages/shared/src/common.ts (PaginatedResponse — envelope field names)
    - apps/api/src/recipes/dto/recipe-query.dto.ts (query param defaults and validation)
  </read_first>
  <action>
Create `skills/recipe-manager/recipes_search.md` with these exact sections:

**Title:** `# Search and list recipes`

**Section 1: Endpoint**
```
GET /api/recipes
```
Authentication required (see `shared.md`). Returns recipes belonging to the authenticated user's household.

**Section 2: Query parameters**
A Markdown table with these exact rows:

| Param | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| search | string | No | — | Case-insensitive substring match on recipe name |
| foodId | string (UUID) | No | — | Filter to recipes containing an ingredient with this food ID |
| sort | enum | No | createdAt | One of: `name`, `createdAt`, `updatedAt`, `random` |
| order | enum | No | desc | One of: `asc`, `desc`. Ignored when sort is `random`. |
| page | integer | No | 1 | 1-based page number |
| pageSize | integer | No | 20 | Items per page (max: 100) |

**Section 3: When to use search vs foodId**
- Use `search` to find recipes by name (e.g., "tortilla", "pollo").
- Use `foodId` to find recipes that use a specific ingredient. Obtain the food ID from `GET /api/foods?search=<name>` (see `recipes_create.md`).
- Both can be combined: `?search=tortilla&foodId=<uuid>` returns recipes named "tortilla" that also contain the specified food.

**Section 4: Response**
The response is a paginated envelope (see `shared.md` for envelope shape). Each item in `items` has this shape:

```json
{
  "id": "a1b2c3d4-...",
  "name": "Tortilla espanola",
  "slug": "tortilla-espanola",
  "description": "Receta tradicional",
  "servingsQty": 4,
  "servingsUnit": "porciones",
  "shareToken": null,
  "createdAt": "2026-03-01T10:00:00.000Z",
  "updatedAt": "2026-03-15T14:30:00.000Z",
  "imageCount": 2,
  "coverImageUrl": "/uploads/abc123.jpg"
}
```

Field types table:

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | string (UUID) | No | Recipe ID — use for `GET /api/recipes/:id` |
| name | string | No | Recipe name |
| slug | string | No | URL-friendly name |
| description | string | Yes | Recipe description |
| servingsQty | number | Yes | Number of servings |
| servingsUnit | string | Yes | Servings unit label |
| shareToken | string | Yes | Non-null if recipe has been shared |
| createdAt | string (ISO 8601) | No | Creation timestamp |
| updatedAt | string (ISO 8601) | No | Last update timestamp |
| imageCount | number | No | Total number of images attached |
| coverImageUrl | string | Yes | URL of the first image, or null if no images |

**Section 5: Example**

Request:
```
GET /api/recipes?search=tortilla&sort=name&order=asc&page=1&pageSize=10
```

Response:
```json
{
  "items": [
    {
      "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
      "name": "Tortilla espanola",
      "slug": "tortilla-espanola",
      "description": "Receta tradicional con patatas",
      "servingsQty": 4,
      "servingsUnit": "porciones",
      "shareToken": null,
      "createdAt": "2026-03-01T10:00:00.000Z",
      "updatedAt": "2026-03-15T14:30:00.000Z",
      "imageCount": 1,
      "coverImageUrl": "/uploads/abc123.jpg"
    }
  ],
  "total": 1,
  "page": 1,
  "perPage": 10
}
```

Note: the response envelope uses `perPage` (not `pageSize`). The query parameter is `pageSize`; the response field is `perPage`.

**Section 6: Cross-references**
- To fetch the full detail of a recipe, use the `id` from a list item with `GET /api/recipes/:id` (see `recipes_get.md`).
- To obtain a `foodId` for filtering, use `GET /api/foods?search=<name>` (see `recipes_create.md`).
  </action>
  <verify>
    <automated>grep "GET /api/recipes" skills/recipe-manager/recipes_search.md && grep "pageSize" skills/recipe-manager/recipes_search.md && grep "perPage" skills/recipe-manager/recipes_search.md && grep "foodId" skills/recipe-manager/recipes_search.md && grep "random" skills/recipe-manager/recipes_search.md && grep "coverImageUrl" skills/recipe-manager/recipes_search.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/recipe-manager/recipes_search.md` exists
    - Contains `GET /api/recipes` (endpoint)
    - Contains all 6 query params: search, foodId, sort, order, page, pageSize
    - Contains all 4 sort values: name, createdAt, updatedAt, random
    - Contains the RecipeListItem fields: id, name, slug, description, servingsQty, servingsUnit, shareToken, createdAt, updatedAt, imageCount, coverImageUrl
    - Contains `perPage` in response example (correct pagination field)
    - Contains at least one full JSON request/response example
    - Contains cross-reference to `recipes_get.md`
    - Contains cross-reference to `shared.md`
  </acceptance_criteria>
  <done>recipes_search.md documents GET /api/recipes with all 6 query params (correct defaults), full RecipeListItem response shape with nullable annotations, and example request/response with correct perPage field</done>
</task>

<task type="auto">
  <name>Task 2: Create recipes_get.md</name>
  <files>skills/recipe-manager/recipes_get.md</files>
  <read_first>
    - plans/02_Skill/skill-bundle-spec.md (canonical requirements for recipes_get.md)
    - packages/shared/src/api/recipes.ts (RecipeDetailResponse, SectionResponse, IngredientResponse, StepResponse, ImageResponse — source of truth)
  </read_first>
  <action>
Create `skills/recipe-manager/recipes_get.md` with these exact sections:

**Title:** `# Get recipe detail`

**Section 1: Endpoint**
```
GET /api/recipes/:id
```
Authentication required (see `shared.md`). Returns the full recipe if it belongs to the authenticated user's household.

- `200` — success, returns full recipe detail
- `403` — recipe belongs to a different household
- `404` — recipe not found

**Section 2: Response shape**

Top-level fields table:

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | string (UUID) | No | Recipe ID |
| householdId | string (UUID) | No | Owning household ID |
| createdById | string (UUID) | No | User who created the recipe |
| name | string | No | Recipe name |
| slug | string | No | URL-friendly name |
| description | string | Yes | Recipe description |
| servingsQty | number | Yes | Number of servings |
| servingsUnit | string | Yes | Servings unit label (e.g., "porciones") |
| prepTime | number | Yes | Preparation time in minutes |
| cookTime | number | Yes | Cooking time in minutes |
| totalTime | number | Yes | Total time in minutes (may be null even if prepTime and cookTime are set) |
| performTime | number | Yes | Active hands-on time in minutes |
| sourceUrl | string | Yes | Original source URL |
| isLocked | boolean | No | If true, recipe cannot be edited |
| shareToken | string | Yes | Non-null if recipe has a public share link |
| createdAt | string (ISO 8601) | No | Creation timestamp |
| updatedAt | string (ISO 8601) | No | Last update timestamp |
| sections | array | No | Ingredient sections (see below) |
| steps | array | No | Instruction steps (see below) |
| images | array | No | Recipe images (see below) |

**Section 3: Nested objects**

**sections[].** — ingredient sections:

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | string (UUID) | No | Section ID |
| title | string | Yes | Section title (null for the default section) |
| order | number | No | Display order (0-based) |
| ingredients | array | No | Ingredients in this section |

**sections[].ingredients[].** — ingredients:

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | string (UUID) | No | Ingredient ID |
| foodId | string (UUID) | No | Food reference ID |
| foodName | string | No | Food display name |
| unitId | string (UUID) | Yes | Unit reference ID |
| unitName | string | Yes | Unit display name |
| quantity | number | Yes | Amount |
| note | string | Yes | Additional note (e.g., "finely chopped") |
| order | number | No | Display order within section (0-based) |

**steps[].** — instruction steps:

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | string (UUID) | No | Step ID |
| title | string | Yes | Optional step title |
| body | string | No | Instruction text |
| order | number | No | Display order (0-based) |

**images[].** — recipe images:

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | string (UUID) | No | Image ID |
| url | string | No | Image URL path (e.g., `/uploads/abc123.jpg`) — serve from base URL |
| order | number | No | Display order (0-based) |
| createdAt | string (ISO 8601) | No | Upload timestamp |

**Section 4: Example**

Request:
```
GET /api/recipes/a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

Response:
```json
{
  "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "householdId": "h1h2h3h4-...",
  "createdById": "u1u2u3u4-...",
  "name": "Tortilla espanola",
  "slug": "tortilla-espanola",
  "description": "Receta tradicional con patatas",
  "servingsQty": 4,
  "servingsUnit": "porciones",
  "prepTime": 15,
  "cookTime": 20,
  "totalTime": 35,
  "performTime": null,
  "sourceUrl": null,
  "isLocked": false,
  "shareToken": null,
  "createdAt": "2026-03-01T10:00:00.000Z",
  "updatedAt": "2026-03-15T14:30:00.000Z",
  "sections": [
    {
      "id": "s1s2s3s4-...",
      "title": null,
      "order": 0,
      "ingredients": [
        {
          "id": "i1i2i3i4-...",
          "foodId": "f1f2f3f4-...",
          "foodName": "Huevo",
          "unitId": "u1u2u3u4-...",
          "unitName": "unidad",
          "quantity": 6,
          "note": null,
          "order": 0
        },
        {
          "id": "i5i6i7i8-...",
          "foodId": "f5f6f7f8-...",
          "foodName": "Patata",
          "unitId": "u5u6u7u8-...",
          "unitName": "kg",
          "quantity": 0.5,
          "note": "cortadas finas",
          "order": 1
        }
      ]
    }
  ],
  "steps": [
    {
      "id": "st1st2st3-...",
      "title": null,
      "body": "Batir los huevos con sal.",
      "order": 0
    },
    {
      "id": "st4st5st6-...",
      "title": null,
      "body": "Freir las patatas en aceite hasta dorar.",
      "order": 1
    }
  ],
  "images": [
    {
      "id": "im1im2im3-...",
      "url": "/uploads/abc123.jpg",
      "order": 0,
      "createdAt": "2026-03-01T10:05:00.000Z"
    }
  ]
}
```

**Section 5: Cross-references**
- The `id` from this response is used as `:id` in all sub-resource endpoints: sections, ingredients, steps, and images (see `recipes_edit.md`, `recipes_image.md`).
- To find a recipe `id`, search via `GET /api/recipes` (see `recipes_search.md`).
- Sub-resource `id` values (section, ingredient, step, image) are used for update and delete operations in `recipes_edit.md` and `recipes_image.md`.
  </action>
  <verify>
    <automated>grep "GET /api/recipes/:id" skills/recipe-manager/recipes_get.md && grep "sections" skills/recipe-manager/recipes_get.md && grep "ingredients" skills/recipe-manager/recipes_get.md && grep "steps" skills/recipe-manager/recipes_get.md && grep "images" skills/recipe-manager/recipes_get.md && grep "foodName" skills/recipe-manager/recipes_get.md && grep "unitName" skills/recipe-manager/recipes_get.md && grep "isLocked" skills/recipe-manager/recipes_get.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `skills/recipe-manager/recipes_get.md` exists
    - Contains `GET /api/recipes/:id` (endpoint)
    - Contains all top-level RecipeDetailResponse fields: id, householdId, createdById, name, slug, description, servingsQty, servingsUnit, prepTime, cookTime, totalTime, performTime, sourceUrl, isLocked, shareToken, createdAt, updatedAt
    - Contains nested sections table with fields: id, title, order, ingredients
    - Contains nested ingredients table with fields: id, foodId, foodName, unitId, unitName, quantity, note, order
    - Contains nested steps table with fields: id, title, body, order
    - Contains nested images table with fields: id, url, order, createdAt
    - Nullable fields are explicitly marked (description, servingsQty, servingsUnit, prepTime, cookTime, totalTime, performTime, sourceUrl, shareToken, unitId, unitName, quantity, note, title on sections and steps)
    - Contains at least one full JSON example response
    - Contains cross-reference to `recipes_search.md` and `recipes_edit.md`
  </acceptance_criteria>
  <done>recipes_get.md documents GET /api/recipes/:id with full nested response shape (sections with ingredients, steps, images), all nullable fields annotated, example response, and cross-references to related skill files</done>
</task>

</tasks>

<verification>
1. `ls skills/recipe-manager/` shows recipes_search.md and recipes_get.md
2. recipes_search.md documents all 6 query params with correct defaults
3. recipes_search.md response uses `perPage` (not `pageSize`)
4. recipes_get.md documents all 4 nested object types (sections, ingredients, steps, images)
5. All nullable fields are annotated as nullable in both files
</verification>

<success_criteria>
- An agent reading recipes_search.md can construct GET /api/recipes with any valid combination of query params and parse the paginated response
- An agent reading recipes_get.md can call GET /api/recipes/:id and extract every nested object (sections with ingredients, steps, images) with correct field names and types
</success_criteria>

<output>
After completion, create `.planning/phases/13-skill-bundle-foundation-read-operations/13-02-SUMMARY.md`
</output>

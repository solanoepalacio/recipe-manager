---
phase: 12-frontend-admin-panel
plan: 04
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - apps/web/src/app/(admin)/admin/panel/foods/page.tsx
  - apps/web/src/app/(admin)/admin/panel/units/page.tsx
  - apps/web/tests/admin/foods-section.test.tsx
  - apps/web/tests/admin/units-section.test.tsx
autonomous: true
requirements:
  - ADM-03
  - ADM-04

must_haves:
  truths:
    - "Admin can view paginated list of foods"
    - "Admin can create, edit, and delete foods"
    - "Admin can view paginated list of units"
    - "Admin can create, edit, and delete units"
  artifacts:
    - path: "apps/web/src/app/(admin)/admin/panel/foods/page.tsx"
      provides: "Foods CRUD section"
      contains: "AdminTable"
    - path: "apps/web/src/app/(admin)/admin/panel/units/page.tsx"
      provides: "Units CRUD section"
      contains: "AdminTable"
  key_links:
    - from: "foods/page.tsx"
      to: "/admin/foods"
      via: "adminApi CRUD"
      pattern: "adminApi\\.(get|post|patch|delete).*admin/foods"
    - from: "units/page.tsx"
      to: "/admin/units"
      via: "adminApi CRUD"
      pattern: "adminApi\\.(get|post|patch|delete).*admin/units"
---

<objective>
Build the Foods and Units management sections: paginated lists with create, edit, and delete for both food items and measurement units.

Purpose: ADM-03 and ADM-04 let admin manage the reference data (foods, units) used by recipe ingredients. These are simpler CRUD sections with fewer fields.
Output: Fully functional Foods and Units admin pages.
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-frontend-admin-panel/12-CONTEXT.md
@.planning/phases/12-frontend-admin-panel/12-RESEARCH.md
@.planning/phases/12-frontend-admin-panel/12-UI-SPEC.md
@.planning/phases/12-frontend-admin-panel/12-02-SUMMARY.md

<interfaces>
From packages/shared/src/api/admin.ts:
```typescript
export interface AdminFoodResponse {
  id: string; name: string; createdAt: string; updatedAt: string;
}
export interface AdminUnitResponse {
  id: string; name: string; abbreviation: string | null;
  createdAt: string; updatedAt: string;
}
```

From packages/shared/src/common.ts:
```typescript
export interface PaginatedResponse<T> {
  data: T[]; total: number; page: number; perPage: number; totalPages: number;
}
```

From apps/web/src/components/admin/AdminTable.tsx:
```typescript
export interface AdminTableColumn<T> { key: string; label: string; render?: (row: T) => React.ReactNode; }
export function AdminTable<T>({ columns, rows, getRowKey, actions, emptyMessage }: AdminTableProps<T>): JSX.Element;
```

From apps/web/src/components/admin/AdminForm.tsx:
```typescript
export function AdminForm({ title, onSubmit, onCancel, submitLabel, isPending, children }: AdminFormProps): JSX.Element;
```

From apps/web/src/lib/query-keys.ts:
```typescript
queryKeys.admin.foods.all     // ['admin', 'foods']
queryKeys.admin.foods.list(p) // ['admin', 'foods', 'list', p]
queryKeys.admin.units.all     // ['admin', 'units']
queryKeys.admin.units.list(p) // ['admin', 'units', 'list', p]
```

API endpoints:
- GET /admin/foods?page=1&perPage=10 -> PaginatedResponse<AdminFoodResponse>
- POST /admin/foods { name } -> AdminFoodResponse
- PATCH /admin/foods/:id { name } -> AdminFoodResponse
- DELETE /admin/foods/:id -> void
- GET /admin/units?page=1&perPage=10 -> PaginatedResponse<AdminUnitResponse>
- POST /admin/units { name, abbreviation? } -> AdminUnitResponse
- PATCH /admin/units/:id { name?, abbreviation? } -> AdminUnitResponse
- DELETE /admin/units/:id -> void
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foods management section</name>
  <files>
    apps/web/src/app/(admin)/admin/panel/foods/page.tsx,
    apps/web/tests/admin/foods-section.test.tsx
  </files>
  <read_first>
    apps/web/src/components/admin/AdminTable.tsx,
    apps/web/src/components/admin/AdminForm.tsx,
    apps/web/src/components/ui/ConfirmDialog.tsx,
    apps/web/src/components/recipes/PaginationControls.tsx,
    apps/web/src/lib/admin-api-client.ts,
    apps/web/src/lib/query-keys.ts,
    packages/shared/src/api/admin.ts,
    .planning/phases/12-frontend-admin-panel/12-UI-SPEC.md
  </read_first>
  <action>
    Create `apps/web/src/app/(admin)/admin/panel/foods/page.tsx`:
    - `'use client'` directive
    - State: `page` (1), `perPage` (10), `showForm` (false), `editingFood` (AdminFoodResponse | null), `deletingId` (string | null)
    - Query: `useQuery({ queryKey: queryKeys.admin.foods.list({ page, perPage }), queryFn: () => adminApi.get<PaginatedResponse<AdminFoodResponse>>(`/admin/foods?page=${page}&perPage=${perPage}`) })`
    - Mutations:
      - Create: `adminApi.post<AdminFoodResponse>('/admin/foods', { name })`, onSuccess: invalidate `queryKeys.admin.foods.all`, close form, `toast.success('Alimento creado.')`
      - Update: `adminApi.patch<AdminFoodResponse>(`/admin/foods/${id}`, { name })`, onSuccess: invalidate, close form, `toast.success('Alimento actualizado.')`
      - Delete: `adminApi.delete(`/admin/foods/${id}`)`, onSuccess: invalidate, clear deletingId, `toast.success('Alimento eliminado.')`
    - Section header: `<h1 className="text-[22px] font-semibold text-foreground" style={{ letterSpacing: '-0.3px' }}>Alimentos</h1>` + "Crear alimento" button (accent)
    - AdminTable columns:
      - `{ key: 'name', label: 'Nombre' }`
      - `{ key: 'createdAt', label: 'Creado', render: (f) => new Date(f.createdAt).toLocaleDateString('es') }`
    - Actions: "Editar" (text-foreground), "Eliminar" (text-destructive)
    - Delete: `<ConfirmDialog message="Eliminar este alimento?" onConfirm={...} onCancel={...} />`
    - Create/Edit form via `AdminForm`: single field — name (required, `<input type="text" id="food-name" required />`)
    - Pagination: `pageSizeOptions={[10, 25, 50]}`
    - Empty state: "Sin alimentos. Anade los primeros."
    - Loading: Skeleton rows while isLoading

    Create `apps/web/tests/admin/foods-section.test.tsx`:
    - Mock adminApi
    - Test: renders "Alimentos" heading
    - Test: renders food rows
    - Test: shows create form with name field
    - Test: shows ConfirmDialog when delete clicked
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager/apps/web && npx tsc --noEmit && yarn test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - foods/page.tsx contains `'use client'`
    - foods/page.tsx contains `adminApi.get` to `/admin/foods`
    - foods/page.tsx contains `adminApi.post` to `/admin/foods`
    - foods/page.tsx contains `adminApi.patch` to `/admin/foods/`
    - foods/page.tsx contains `adminApi.delete` to `/admin/foods/`
    - foods/page.tsx contains "Alimentos" heading
    - foods/page.tsx contains "Crear alimento" button text
    - foods/page.tsx contains `AdminTable` and `AdminForm`
    - foods/page.tsx contains `PaginationControls` with `pageSizeOptions={[10, 25, 50]}`
    - foods-section.test.tsx file exists
  </acceptance_criteria>
  <done>Foods section shows paginated list, create/edit/delete work, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Units management section</name>
  <files>
    apps/web/src/app/(admin)/admin/panel/units/page.tsx,
    apps/web/tests/admin/units-section.test.tsx
  </files>
  <read_first>
    apps/web/src/app/(admin)/admin/panel/foods/page.tsx,
    apps/web/src/components/admin/AdminTable.tsx,
    apps/web/src/components/admin/AdminForm.tsx,
    apps/web/src/components/ui/ConfirmDialog.tsx,
    apps/web/src/lib/admin-api-client.ts,
    apps/web/src/lib/query-keys.ts,
    packages/shared/src/api/admin.ts,
    .planning/phases/12-frontend-admin-panel/12-UI-SPEC.md
  </read_first>
  <action>
    Create `apps/web/src/app/(admin)/admin/panel/units/page.tsx`:
    - `'use client'` directive
    - Nearly identical to foods section but with an additional `abbreviation` field
    - State: `page`, `perPage`, `showForm`, `editingUnit` (AdminUnitResponse | null), `deletingId`
    - Query: `adminApi.get<PaginatedResponse<AdminUnitResponse>>(`/admin/units?page=${page}&perPage=${perPage}`)`
    - Mutations:
      - Create: `adminApi.post('/admin/units', { name, abbreviation: abbreviation || null })`, toast "Unidad creada."
      - Update: `adminApi.patch(`/admin/units/${id}`, { name, abbreviation: abbreviation || null })`, toast "Unidad actualizada."
      - Delete: `adminApi.delete(`/admin/units/${id}`)`, toast "Unidad eliminada."
    - Section header: `<h1>Unidades</h1>` + "Crear unidad" button
    - AdminTable columns:
      - `{ key: 'name', label: 'Nombre' }`
      - `{ key: 'abbreviation', label: 'Abreviatura', render: (u) => u.abbreviation ?? '—' }`
      - `{ key: 'createdAt', label: 'Creado', render: (u) => new Date(u.createdAt).toLocaleDateString('es') }`
    - Actions: "Editar", "Eliminar"
    - Delete: `<ConfirmDialog message="Eliminar esta unidad?" />`
    - Create/Edit form: two fields — name (required), abbreviation (optional)
    - Pagination: `pageSizeOptions={[10, 25, 50]}`
    - Empty state: "Sin unidades. Anade las primeras."

    Create `apps/web/tests/admin/units-section.test.tsx`:
    - Mock adminApi
    - Test: renders "Unidades" heading
    - Test: renders unit rows with abbreviation column
    - Test: create form has name and abbreviation fields
    - Test: delete shows ConfirmDialog
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager/apps/web && npx tsc --noEmit && yarn test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - units/page.tsx contains `'use client'`
    - units/page.tsx contains `adminApi.get` to `/admin/units`
    - units/page.tsx contains `adminApi.post` to `/admin/units`
    - units/page.tsx contains `adminApi.patch` to `/admin/units/`
    - units/page.tsx contains `adminApi.delete` to `/admin/units/`
    - units/page.tsx contains "Unidades" heading
    - units/page.tsx contains "Crear unidad" button text
    - units/page.tsx contains `AdminTable` and `AdminForm`
    - units/page.tsx form has both `name` and `abbreviation` fields
    - units/page.tsx contains `PaginationControls` with `pageSizeOptions={[10, 25, 50]}`
    - units-section.test.tsx file exists
  </acceptance_criteria>
  <done>Units section shows paginated list with abbreviation column, create/edit/delete work, tests pass</done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` passes
2. `cd apps/web && yarn test` passes
3. Foods section at /admin/panel/foods shows paginated table with CRUD
4. Units section at /admin/panel/units shows paginated table with abbreviation column and CRUD
</verification>

<success_criteria>
- Admin can view, create, edit, delete foods at /admin/panel/foods
- Admin can view, create, edit, delete units at /admin/panel/units (with abbreviation field)
- Both use pageSizeOptions [10, 25, 50]
- Both show appropriate empty state messages
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-admin-panel/12-04-SUMMARY.md`
</output>

---
phase: 8
slug: frontend-recipe-list-detail-cook-mode
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-18
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for Phase 8: Recipe List, Recipe Detail, and Cook Mode pages.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | No `components.json` found; no shadcn; Tailwind v4 CSS variables in `globals.css` |
| Preset | not applicable | Manual token definitions via `@theme {}` block |
| Component library | none | Custom components built with Tailwind v4 utility classes |
| Icon library | Lucide React ^0.577.0 | RESEARCH.md + existing codebase (`TopBar.tsx`, `AppShell.tsx`) |
| Font | Outfit (next/font/google, self-hosted) | globals.css `--font-family-sans: 'Outfit', sans-serif` |

---

## Spacing Scale

Declared values (must be multiples of 4, except project-adopted 12px and 20px documented below):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gaps (`gap-1`), inline label gaps |
| sm | 8px | Chip gaps, step-list vertical padding, icon gaps inside buttons |
| compact | 12px | **Project-adopted value (`py-3`).** Compact vertical rhythm: detail top bar `py-3`, cook mode top bar `py-3`, info grid vertical padding, search actions row bottom padding. Established across codebase. |
| md | 16px | Search bar horizontal padding, card padding, section content padding, recipe card content gap |
| page | 20px | **Project-adopted value (`px-5`).** Page horizontal padding on all three pages, top-bar horizontal padding, cook mode step row horizontal padding. Established as the standard page-edge spacing across the codebase. |
| lg | 24px | Drawer header padding-top |
| xl | 32px | Drawer header padding-top top section |
| 3xl | 48px | Reserved in scale |
| 4xl | 64px | FAB bottom clearance (scroll padding on recipe list) |

**Standard set: 4, 8, 12, 16, 20, 24, 32, 48, 64**

The 8-point canonical multiples are 4, 8, 16, 24, 32, 48, 64. This project additionally adopts 12px and 20px as standard values based on established codebase patterns (`py-3` for compact vertical rhythm, `px-5` for page horizontal padding). All spacing declarations in this spec use values from the above standard set.

Exceptions (fixed dimensions — not spacing tokens):
- Recipe card thumbnail: 72px wide × 68px tall (fixed dimensions from wireframe 02)
- FAB: 52×52px touch target at `bottom-7 right-5` — already implemented in AppShell
- Cook mode current step left border: `border-l-[2.5px]` with `pl-[17.5px]` compensation (17.5px = 20px - 2.5px border width; visual result is 20px page alignment)

---

## Typography

Exactly 4 sizes declared. Exactly 2 weights in use: **400 (regular)** and **600 (semibold)**.

| Role | Size | Weight | Line Height | Letter Spacing | Usage |
|------|------|--------|-------------|----------------|-------|
| Body | 15px | 400 | 1.5 | -0.1px | Recipe card name, ingredient list items, step list items (detail view), info-grid values |
| Label | 13px | 600 | 1.4 | 0 | Search action buttons (`font-semibold`), chip text, meta time labels, section headers (accordion titles), done step text, info-grid labels, pagination controls |
| Heading | 20px | 600 | 1.2 | -0.3px | Recipe title on detail page header; TopBar title "Recetas" (already implemented at 18px — update to 20px on next touch, or leave as-is if component is shared; note below) |
| Cook step body | 16px | 400 | 1.55 | -0.1px | Cook mode pending step text — larger for in-kitchen readability |

**Weight assignment for formerly `font-medium` (500) elements:**
- All search action labels, chip text, button labels, drawer nav items, pagination prev/next labels → `font-semibold` (600). These are interactive affordance elements that require visual distinction from body content.
- Done step text → `font-normal` (400) with `text-placeholder` color. The color de-emphasizes; weight does not need to add emphasis on top.
- Cook mode exit label "× Salir" → `font-normal` (400) with `text-secondary` color.
- Detail top-bar recipe name (center, scroll-aware) → `font-semibold` (600), same as heading role.

**TopBar note:** The existing `TopBar.tsx` component renders its title at 18px weight 600. That component is shared and was implemented in Phase 7. The Phase 8 typography scale declares 20px for headings. The TopBar should be updated to `text-[20px]` on its next touch; until then the discrepancy is documented. Do NOT declare 18px as a fifth size — it collapses into 20px.

---

## Color

All tokens are defined in `apps/web/src/app/globals.css` via Tailwind v4 `@theme {}` block.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `bg-background` | `#FAFAF7` | Page background, top bar (list + cook mode), recipe list, content areas |
| Secondary (30%) | `bg-subtle` | `#F4F2ED` | Search bar fill, section accordion headers, chip active background, done step background, Skeleton pulse |
| Surface variant | `bg-sand` | `#E8E1D5` | Recipe detail top bar, recipe card thumbnail placeholder, drawer header |
| Accent (10%) | `bg-accent` / `text-accent` | `#5EBD6A` | FAB background, active drawer nav underline, share link text, active chip text (when accent-styled) |
| Foreground (primary text) | `text-foreground` | `#2C2C2A` | Recipe names, headings, step numbers circle bg, all primary text |
| Secondary text | `text-secondary` | `#8A8680` | Meta labels, search action text, search icon, info-grid labels, drawer greeting, cook mode exit text |
| Placeholder / muted | `text-placeholder` | `#C8C4BD` | Search input placeholder, done step text, step-check circle bg, delete/drag handle icons |
| Border | `border-border` | `#E0DCD5` | Dividers, chip outlines, info-grid cell separators, cook mode top bar bottom border |
| Subtle border | `border-subtle` | `#F4F2ED` | Recipe card bottom border, ingredient/step row bottom borders |
| Destructive | `text-destructive` | `#D94F4F` | Drawer logout text only — no destructive actions in Phase 8 |

Accent (`#5EBD6A`) is reserved for:
- FAB (Crear receta) background
- Active Drawer navigation item underline (`border-b-2 border-accent`)
- Share link text color on recipe detail page

The 60% / 30% / 10% split maps to: `#FAFAF7` dominant surfaces, `#F4F2ED` + `#E8E1D5` secondary surfaces (cards, headers, bars), `#5EBD6A` accent touches (FAB, active state, share link).

---

## Component Specifications

### 1. Recipe List Page (`/recipes`)

**Focal point:** The recipe card list is the primary focal point of this page. All chrome (search bar, filter row) is subordinate — kept compact and in `text-secondary` to direct eye weight to the card names and thumbnails in `text-foreground`.

#### Page Layout
- Background: `bg-background`
- Horizontal page padding: `px-5` (20px) on all sections
- Bottom padding: `pb-20` (80px) to clear FAB

#### TopBar (existing component — no change)
- Background: `bg-background`
- Title text: "Recetas", 20px, weight 600, `text-foreground`, letter-spacing -0.3px
- Padding: `px-5 py-4` (20px horizontal, 16px vertical)
- Left: hamburger menu icon (Lucide `Menu`, 24px, strokeWidth 2)

#### Search Bar
- Container padding: `px-5 pb-3` (below top bar)
- Input wrapper: `bg-subtle rounded-xl px-4 py-3 flex items-center gap-2` (8px gap)
- Search icon: Lucide `Search`, 18px, strokeWidth 2, `text-secondary`
- Input: 15px, `text-placeholder` when empty, `text-foreground` when filled, `bg-transparent border-none outline-none flex-1`
- Placeholder text: "Buscar recetas..."

#### Filter Actions Row
- Container: `flex gap-4 px-5 pb-3 pt-1` (12px bottom, nearest multiple of 4 to wireframe)
- Each action: 13px, weight 600, `text-secondary`, `flex items-center gap-1`
- Sort icon: Lucide `ArrowUpDown`, 14px, strokeWidth 2
- Filter icon: Lucide `SlidersHorizontal`, 14px, strokeWidth 2
- Action labels: "Ordenar" | "Filtrar por ingredientes"
- Active state (when filter/sort applied): `text-foreground` (darken from `text-secondary`)

#### Sort Options (dropdown or inline selector)
- Options: Nombre A–Z | Nombre Z–A | Más recientes | Más antiguos | Aleatorio
- Maps to API: `sort=name&order=asc` | `sort=name&order=desc` | `sort=updatedAt&order=desc` | `sort=updatedAt&order=asc` | `sort=random`

#### Food Filter (dropdown)
- Populated from `GET /api/foods` via separate `useQuery`
- Placeholder: "Todos los ingredientes"
- Selected state: shows food name, applies `foodId` query param

#### Recipe Card
- Container: `flex items-center gap-4 py-[10px] border-b border-subtle` — gap-4 = 16px (nearest multiple of 4 to wireframe 14px)
- Last card: no bottom border
- Thumbnail: `w-[72px] h-[68px] rounded-[10px] bg-sand flex-shrink-0` (sand placeholder for Phase 8 — no real image URL in `RecipeListItem`)
- When `imageCount > 0`: show placeholder with a small `text-secondary` image icon centered; when `imageCount === 0`: plain sand placeholder
- Recipe name: 15px, weight 600, `text-foreground`, letter-spacing -0.2px
- Time meta: 13px, `text-secondary`, `flex items-center gap-1`, Lucide `Clock` icon 13px strokeWidth 2
- Time format: display `totalTime` or `cookTime` if available; omit if null

#### Pagination Controls
- Position: below recipe list, above FAB clearance
- Layout: `flex items-center justify-between px-5 py-4`
- Prev/Next buttons: `flex items-center gap-1 text-[13px] font-semibold text-secondary` — disabled state: `text-placeholder cursor-not-allowed`
- Page indicator: `text-[13px] text-secondary` — "Página N de M"
- Page size selector: `text-[13px] text-secondary` — options: 10, 20, 50

#### Empty State (no recipes)
- See Copywriting Contract section

#### Loading State
- Skeleton cards: 5 rows of `Skeleton` component (existing) at `h-[68px] rounded-[10px]` for thumbnail + `Skeleton h-4 w-32` for name + `Skeleton h-3 w-16` for time

---

### 2. Recipe Detail Page (`/recipes/[slug]`)

**Focal point:** Primary focal point is the recipe title (20px heading, weight 600, `text-foreground`) and the "Iniciar receta" button in the sticky header below the hero. These two elements are the first visual anchors after the hero image and communicate the page's core purpose — identity and action.

#### Top Bar (detail variant — differs from AppShell TopBar)
- Background: `bg-sand` (not `bg-background`)
- Padding: `px-5 py-3` (20px horizontal, 12px vertical)
- Left: Lucide `ArrowLeft`, 22px, strokeWidth 2, `text-foreground` — navigates back to list
- Center: recipe name, 15px, weight 600, `text-placeholder` when hero visible / `text-foreground` when scrolled past hero
- Right: Lucide `EllipsisVertical`, 20px, strokeWidth 2, `text-foreground`, `aria-label="Más opciones"` — overflow menu (deferred to Phase 9; render icon as placeholder button with this aria-label)

#### Hero Image
- Height: 220px, full width
- `object-fit: cover`
- When no images: omit hero block entirely (Phase 8 may have no images — display is optional)

#### Recipe Header (sticky)
- Background: `bg-background`
- Padding: `px-5 pt-4 pb-3`
- Recipe title: 20px, weight 600, `text-foreground`, letter-spacing -0.3px, `mb-3`
- Actions row: `flex items-center justify-between`
  - Share link: Lucide `ExternalLink` 14px + "Compartir", 13px, weight 600, `text-accent` (accent reserved for this)
  - "Iniciar receta" button: `bg-foreground text-background rounded-[20px] px-5 py-2 text-[13px] font-semibold flex items-center gap-2` + Lucide `CookingPot` 16px — navigates to `/recipes/[slug]/cook`
  - "Editar receta" button: `border border-border text-foreground rounded-[20px] px-5 py-2 text-[13px] font-semibold` (rendered in Phase 8 but wires to Phase 9 edit route — disable or hide if edit not yet implemented; show as `text-placeholder border-subtle` disabled state)

#### Section Accordion
- Header: `flex items-center gap-2 px-5 py-3 bg-subtle cursor-pointer`
- Chevron icon: Lucide `ChevronDown` (expanded) / `ChevronRight` (collapsed), 16px, strokeWidth 2, `text-secondary`
- Title: 13px, weight 600, `text-foreground`
- Sections: Información | Ingredientes | Instrucciones
- Default state: all three sections expanded on initial render

#### Info Grid (inside Información section)
- Container: `flex px-5 py-3 border-b border-subtle` (12px vertical padding)
- Each cell: `flex-1 text-center relative`
- Vertical divider between cells: CSS `::before` pseudo-element, 1px `bg-border`, `top-1 bottom-1`
- Label: 13px, weight 600, `text-secondary`, `mb-1`
- Value: 15px, weight 600, `text-foreground`
- Labels (Spanish): Preparación | Cocción | Total | Porciones
- Null fields: show "—" when time is null

#### Ingredient List Row
- Container: `flex items-center px-5 py-3 border-b border-subtle gap-2` (8px gap)
- Text: 15px, weight 400, `text-foreground`
- Format: "{qty} {unit} {food}" with optional note in `text-secondary`

#### Instruction Step Row (detail view)
- Container: `flex items-center px-5 py-3 border-b border-subtle gap-2` (8px gap)
- Step number badge: `w-6 h-6 rounded-full bg-foreground text-background text-[13px] font-semibold flex items-center justify-center flex-shrink-0`
- Text: 15px, weight 400, `text-foreground`, `flex-1`

#### Loading State
- Replace accordion sections with `Skeleton` rows during `isLoading`

---

### 3. Cook Mode Page (`/recipes/[slug]/cook`)

**Focal point:** Primary focal point is the current pending step — highlighted with a left border (`border-l-[2.5px] border-border`) and rendered at 16px body text (larger than all other body text in the app) for in-kitchen readability. The "× Salir" exit control is visually de-emphasized (`text-secondary`, weight 400) to keep focus on the step content.

#### Layout Strategy
- Full-screen overlay: `position: fixed; inset: 0; z-index: 100; background: bg-background`
- This covers the AppShell entirely — no route group restructuring needed
- The `(cook)` overlay approach from RESEARCH.md Option C is the implementation

#### Cook Mode Top Bar
- Background: `bg-background`
- Border: `border-b border-border`
- Padding: `px-5 py-3` (20px horizontal, 12px vertical)
- Left: exit control — "× Salir", 13px, weight 400, `text-secondary`, letter-spacing -0.1px — navigates back to detail page via `router.back()`
- Center: recipe title, 15px, weight 600, `text-foreground`, letter-spacing -0.2px, centered via `flex-1 text-center px-3`
- Right: spacer `w-[52px]` to keep title visually centered

#### Step List Container
- `flex-1 overflow-y-auto py-2 relative`
- Bottom fade: `::after` pseudo-element, `h-20` gradient from transparent to `#FAFAF7`, `pointer-events-none`, covers last steps as affordance to scroll

#### Pending Step Row
- Container: `flex gap-4 px-5 py-5 border-b border-subtle items-start` (16px gap)
- Current step additional: `border-l-[2.5px] border-border pl-[17.5px]` (17.5px = 20px - 2.5px border compensation)
- Step number badge: `w-6 h-6 rounded-full bg-foreground text-background text-[13px] font-semibold flex items-center justify-center flex-shrink-0 mt-px`
- Step text: 16px, weight 400, `text-foreground`, line-height 1.55, letter-spacing -0.1px

#### Done Step Row
- Container: `flex gap-4 px-5 h-[52px] items-center bg-subtle border-b border-border` (16px gap)
- Check circle: `w-6 h-6 rounded-full bg-placeholder text-background flex items-center justify-center flex-shrink-0` + Lucide `Check` 13px strokeWidth 2.5
- Text: 13px, weight 400, `text-placeholder`, truncated (`truncate flex-1 min-w-0`)

#### Divider Between Done and Pending Sections
- `h-px bg-border` — rendered once, between last done step and first pending step

#### Interaction: Check-Off to Advance
- Tapping the current step (anywhere on the row, including the step number badge) marks it done and advances `currentStep` index
- No explicit next/prev buttons — the check-off-to-advance pattern is the sole navigation
- All steps are always rendered (done stack at top, pending below divider)
- On last step done: show completion state (see Copywriting Contract)

#### Cook Mode Entry / Exit
- Entry: "Iniciar receta" button on recipe detail page navigates to `/recipes/[slug]/cook`
- Exit: "× Salir" in top bar calls `router.back()` (returns to detail page)

---

## Copywriting Contract

All UI strings are in Spanish per project convention.

| Element | Copy |
|---------|------|
| Page title (recipe list) | "Recetas" |
| Search placeholder | "Buscar recetas..." |
| Sort action label | "Ordenar" |
| Filter action label | "Filtrar por ingredientes" |
| Cook mode entry button | "Iniciar receta" |
| Cook mode exit control | "× Salir" |
| Share link | "Compartir" |
| Edit button | "Editar receta" |
| Pagination prev | "← Anterior" |
| Pagination next | "Siguiente →" |
| Pagination indicator | "Página {N} de {M}" |
| Page size label | "Por página:" |
| Empty state heading (no recipes) | "Sin recetas aún" |
| Empty state body (no recipes) | "Tu hogar no tiene recetas todavía. Toca el botón + para crear la primera." |
| Empty state heading (search/filter no results) | "Sin resultados" |
| Empty state body (search/filter no results) | "No encontramos recetas con esos criterios. Prueba con otra búsqueda o quita los filtros." |
| Error state (list fetch failed) | "No se pudieron cargar las recetas. Comprueba tu conexión e intenta de nuevo." |
| Error state (detail fetch failed) | "No se pudo cargar esta receta. Comprueba tu conexión e intenta de nuevo." |
| Error state (cook mode — recipe not found) | "Receta no encontrada. Vuelve a la lista de recetas." |
| Cook mode completion heading | "¡Listo!" |
| Cook mode completion body | "Has completado todos los pasos." |
| Cook mode completion action | "Volver a la receta" |
| Info grid label — prep | "Preparación" |
| Info grid label — cook | "Cocción" |
| Info grid label — total | "Total" |
| Info grid label — servings | "Porciones" |
| Null time value | "—" |
| Accordion section — info | "Información" |
| Accordion section — ingredients | "Ingredientes" |
| Accordion section — instructions | "Instrucciones" |

Destructive actions in Phase 8: none. No delete, no destructive confirmation needed.

**Cook mode entry button change note:** Previously declared as "Cocinar" (single verb). Changed to "Iniciar receta" (verb + noun) for clarity — communicates intent and target noun more explicitly. This is consistent with the wireframe design intent (the button leads into a distinct mode for a specific recipe).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| Third-party | none declared | not applicable |

No third-party component registries are used in Phase 8. All components are built from scratch using Tailwind v4 utility classes and Lucide React icons.

---

## Interaction States Summary

| Component | States | Behavior |
|-----------|--------|----------|
| Search input | empty (placeholder) / filled (value) / loading | debounce 300ms before query fires |
| Recipe card | default / tap/hover (navigate to detail) | `cursor-pointer`, navigates to `/recipes/[slug]` |
| Sort/Filter action | default / active (filter applied) | `text-secondary` → `text-foreground` when active |
| Food filter dropdown | default / open / selected | selected shows food name; clears with × |
| Pagination prev/next | enabled / disabled | disabled when on first/last page: `text-placeholder cursor-not-allowed` |
| Section accordion | expanded / collapsed | chevron rotates; content block hidden/shown |
| Cook step | pending / current / done | current has left border; done collapses to 52px with check circle |
| Cook step tap | tap current step | advances `currentStep` index; previous step becomes done |
| Cook mode exit | tap "× Salir" | `router.back()` |
| Detail "Iniciar receta" button | default / tap | navigates to `/recipes/[slug]/cook` |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

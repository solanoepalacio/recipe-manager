---
phase: 01-monorepo-shared-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .yarnrc.yml
  - tsconfig.base.json
  - .gitignore
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/nest-cli.json
  - apps/api/prisma/schema.prisma
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/src/app/page.tsx
  - apps/web/src/app/layout.tsx
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - apps/api/tests/app.e2e-spec.ts
  - apps/api/jest.config.ts
  - apps/api/test/jest-e2e.json
autonomous: true
requirements:
  - API-03

must_haves:
  truths:
    - "yarn install succeeds with no errors from the monorepo root"
    - "yarn workspaces foreach run build exits 0 with no TypeScript errors"
    - "apps/api, apps/web, and packages/shared each have a valid package.json with the correct @recipe-manager/* name"
    - "Both apps can reference @recipe-manager/shared via workspace:* protocol and tsconfig paths"
    - "A minimal schema.prisma exists (datasource + generator only) so prisma generate produces a usable client stub"
    - "Wave 0 test infra exists: jest.config.ts, test/jest-e2e.json, and the e2e spec file"
  artifacts:
    - path: "package.json"
      provides: "Yarn v4 workspace root declaring apps/* and packages/*"
      contains: "workspaces"
    - path: ".yarnrc.yml"
      provides: "nodeLinker: node-modules setting for NestJS/Next.js compatibility"
      contains: "nodeLinker: node-modules"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript config with strict mode and decorator support"
      contains: "experimentalDecorators"
    - path: "apps/api/prisma/schema.prisma"
      provides: "Minimal schema for prisma generate to succeed"
      contains: "generator client"
    - path: "apps/api/jest.config.ts"
      provides: "Jest unit test config for apps/api"
    - path: "apps/api/test/jest-e2e.json"
      provides: "Jest e2e test config"
    - path: "apps/api/tests/app.e2e-spec.ts"
      provides: "Smoke test verifying GET /api/docs returns 200"
  key_links:
    - from: "apps/api/tsconfig.json"
      to: "packages/shared/src/index.ts"
      via: "compilerOptions.paths"
      pattern: "@recipe-manager/shared"
    - from: "apps/web/tsconfig.json"
      to: "packages/shared/src/index.ts"
      via: "compilerOptions.paths"
      pattern: "@recipe-manager/shared"
    - from: "apps/api/package.json"
      to: "packages/shared"
      via: "workspace:* dependency"
      pattern: "workspace:\\*"
---

<objective>
Scaffold the complete Yarn v4 monorepo structure with all three workspaces, shared TypeScript config, and the Wave 0 test infrastructure. After this plan, `yarn install` and `yarn workspaces foreach run build` succeed, and both apps can resolve imports from `@recipe-manager/shared`.

Purpose: Every subsequent plan depends on this foundation. The monorepo structure, package names, tsconfig strategy, and workspace protocol are all established here.
Output: Root config files, three workspace package.json/tsconfig.json files, minimal source files for each workspace, minimal schema.prisma stub, and Wave 0 test infra for apps/api.
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@plans/01_App/07_project_structure.md
@plans/01_App/01_tech_stack_and_data_model.md
@.planning/phases/01-monorepo-shared-types/01-CONTEXT.md
@.planning/phases/01-monorepo-shared-types/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Root monorepo config — package.json, .yarnrc.yml, tsconfig.base.json, .gitignore</name>
  <files>
    package.json,
    .yarnrc.yml,
    tsconfig.base.json,
    .gitignore
  </files>
  <read_first>
    - /home/solanoe/code/recipe-manager/package.json (current state — may already have partial config)
    - /home/solanoe/code/recipe-manager/.yarnrc.yml (if exists)
    - /home/solanoe/code/recipe-manager/tsconfig.base.json (if exists)
    - /home/solanoe/code/recipe-manager/plans/01_App/07_project_structure.md (authoritative directory structure)
    - /home/solanoe/code/recipe-manager/.planning/phases/01-monorepo-shared-types/01-RESEARCH.md (Pattern 1, Pattern 7)
  </read_first>
  <action>
Create/update the four root-level config files:

**package.json** (root — must be exactly this structure):
```json
{
  "name": "recipe-manager",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all run build"
  }
}
```

**NOTE:** The existing package.json may have content already. Read it first and PRESERVE any existing `"packageManager"` field (e.g. `"packageManager": "yarn@4.x.x"`). Merge the workspaces and scripts fields in — do not overwrite the whole file blindly.

**.yarnrc.yml** (create or overwrite fully):
```yaml
nodeLinker: node-modules
```
If a `yarnPath` line already exists (e.g. `yarnPath: .yarn/releases/yarn-4.x.x.cjs`), preserve it. The critical line is `nodeLinker: node-modules` — add it if missing.

**tsconfig.base.json** (create at root):
```json
{
  "compilerOptions": {
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "moduleResolution": "node",
    "target": "ES2020",
    "lib": ["ES2020"],
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  }
}
```

**.gitignore** — append the following if not already present (do NOT overwrite; use Read first to check):
```
node_modules/
dist/
.next/
*.env
.env.local
.env.*.local
```
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && cat package.json | grep -q '"workspaces"' && echo "workspaces OK" && cat .yarnrc.yml | grep -q 'nodeLinker: node-modules' && echo "nodeLinker OK" && cat tsconfig.base.json | grep -q 'experimentalDecorators' && echo "decorators OK"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"workspaces": ["apps/*", "packages/*"]`
    - .yarnrc.yml contains `nodeLinker: node-modules`
    - tsconfig.base.json contains `"experimentalDecorators": true`
    - tsconfig.base.json contains `"emitDecoratorMetadata": true`
    - tsconfig.base.json contains `"strict": true`
  </acceptance_criteria>
  <done>Root monorepo config files exist and contain the required settings.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Three workspace scaffolds — package.json, tsconfig.json, minimal source files, prisma stub</name>
  <files>
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/index.ts,
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/api/nest-cli.json,
    apps/api/prisma/schema.prisma,
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/next.config.ts,
    apps/web/src/app/page.tsx,
    apps/web/src/app/layout.tsx
  </files>
  <read_first>
    - /home/solanoe/code/recipe-manager/plans/01_App/07_project_structure.md (authoritative directory structure and package names)
    - /home/solanoe/code/recipe-manager/.planning/phases/01-monorepo-shared-types/01-RESEARCH.md (Pattern 2, Pattern 6, Pitfall 1, Pitfall 2, Pitfall 3)
    - /home/solanoe/code/recipe-manager/.planning/phases/01-monorepo-shared-types/01-CONTEXT.md (apps/web scaffold constraints)
  </read_first>
  <action>
Create all three workspace directories and their config/source files. Run `yarn install` after creating package.json files.

**packages/shared/package.json**:
```json
{
  "name": "@recipe-manager/shared",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
```

**packages/shared/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "CommonJS",
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true
  },
  "include": ["src"]
}
```

**packages/shared/src/index.ts** (empty barrel — type files added in Plan 02):
```typescript
// Shared API boundary types for @recipe-manager/shared
// Type files are added per-domain by each phase.
// Phase 1 (auth/setup/profile/household/common/enums) types are added in Plan 02.
export {};
```

**apps/api/package.json**:
```json
{
  "name": "@recipe-manager/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "nest build",
    "start": "node dist/main",
    "start:dev": "nest start --watch",
    "test": "jest",
    "test:e2e": "jest --config test/jest-e2e.json"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@nestjs/swagger": "^8.0.0",
    "@nestjs/config": "^4.0.0",
    "@prisma/client": "^6.0.0",
    "class-validator": "^0.14.0",
    "class-transformer": "^0.5.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.0.0",
    "swagger-ui-express": "^5.0.0",
    "@recipe-manager/shared": "workspace:*"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "@nestjs/testing": "^11.0.0",
    "@types/node": "^22.0.0",
    "@types/supertest": "^6.0.0",
    "jest": "^29.0.0",
    "prisma": "^6.0.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
```

**apps/api/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "CommonJS",
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "paths": {
      "@recipe-manager/shared": ["../../packages/shared/src/index.ts"]
    }
  },
  "include": ["src", "tests"]
}
```

**apps/api/nest-cli.json**:
```json
{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": {
    "deleteOutDir": true
  }
}
```

**apps/api/prisma/schema.prisma** (minimal — no models yet; Phase 2 adds all models):
```prisma
// This file is managed by Phase 2 (Database Schema + Prisma).
// A minimal datasource + generator block exists here so Phase 1's
// `prisma generate` produces a usable (empty) client stub.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

**apps/web/package.json**:
```json
{
  "name": "@recipe-manager/web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@recipe-manager/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.0.0"
  }
}
```

**apps/web/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "preserve",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"],
      "@recipe-manager/shared": ["../../packages/shared/src/index.ts"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**apps/web/next.config.ts**:
```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {};

export default nextConfig;
```

**apps/web/src/app/layout.tsx** (required by Next.js 15 App Router):
```tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
```

**apps/web/src/app/page.tsx**:
```tsx
export default function Page() {
  return <div>Recipe Manager</div>;
}
```

After creating all files, run from the repo root:
```bash
yarn install
```

Then generate the Prisma client stub (needed for apps/api to compile):
```bash
yarn workspace @recipe-manager/api prisma generate
```
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && cat packages/shared/package.json | grep -q '"@recipe-manager/shared"' && echo "shared name OK" && cat apps/api/package.json | grep -q '"@recipe-manager/api"' && echo "api name OK" && cat apps/web/package.json | grep -q '"@recipe-manager/web"' && echo "web name OK" && cat apps/api/tsconfig.json | grep -q '"@recipe-manager/shared"' && echo "api paths OK" && cat apps/web/tsconfig.json | grep -q '"@recipe-manager/shared"' && echo "web paths OK" && cat apps/api/prisma/schema.prisma | grep -q 'generator client' && echo "prisma stub OK" && test -f apps/web/src/app/layout.tsx && echo "layout OK"</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/package.json contains `"name": "@recipe-manager/shared"` and `"main": "src/index.ts"`
    - apps/api/package.json contains `"name": "@recipe-manager/api"` and `"@recipe-manager/shared": "workspace:*"` in dependencies
    - apps/web/package.json contains `"name": "@recipe-manager/web"` and `"@recipe-manager/shared": "workspace:*"` in dependencies
    - apps/api/tsconfig.json compilerOptions.paths contains `"@recipe-manager/shared": ["../../packages/shared/src/index.ts"]`
    - apps/web/tsconfig.json compilerOptions.paths contains `"@recipe-manager/shared": ["../../packages/shared/src/index.ts"]`
    - apps/api/prisma/schema.prisma contains `generator client` and `datasource db`
    - apps/web/src/app/layout.tsx exists
    - apps/web/src/app/page.tsx exists
    - yarn install exits 0 with no errors
    - yarn workspace @recipe-manager/api prisma generate exits 0
  </acceptance_criteria>
  <done>All three workspaces are scaffolded with correct package names, tsconfig paths for shared package resolution, and yarn install succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wave 0 test infrastructure — jest.config.ts, jest-e2e.json, e2e smoke test scaffold</name>
  <files>
    apps/api/jest.config.ts,
    apps/api/test/jest-e2e.json,
    apps/api/tests/app.e2e-spec.ts
  </files>
  <read_first>
    - /home/solanoe/code/recipe-manager/.planning/phases/01-monorepo-shared-types/01-VALIDATION.md (Wave 0 requirements)
    - /home/solanoe/code/recipe-manager/.planning/phases/01-monorepo-shared-types/01-RESEARCH.md (Validation Architecture section)
    - /home/solanoe/code/recipe-manager/plans/01_App/07_project_structure.md (test directory structure: tests/ for unit, test/ for e2e config)
  </read_first>
  <action>
Create the Jest test infrastructure for apps/api. The e2e spec is a scaffold — the actual NestJS app doesn't exist yet (that's Plan 03), so the test file exists but the import will be wired up in Plan 03.

**apps/api/jest.config.ts** (unit test config):
```typescript
import type { Config } from 'jest';

const config: Config = {
  moduleFileExtensions: ['js', 'json', 'ts'],
  rootDir: 'src',
  testRegex: '.*\\.spec\\.ts$',
  transform: {
    '^.+\\.(t|j)s$': 'ts-jest',
  },
  collectCoverageFrom: ['**/*.(t|j)s'],
  coverageDirectory: '../coverage',
  testEnvironment: 'node',
  moduleNameMapper: {
    '^@recipe-manager/shared(.*)$': '<rootDir>/../../packages/shared/src/index.ts',
  },
};

export default config;
```

**apps/api/test/jest-e2e.json** (e2e test config — NestJS CLI default location):
```json
{
  "moduleFileExtensions": ["js", "json", "ts"],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec.ts$",
  "transform": {
    "^.+\\.(t|j)s$": ["ts-jest", {
      "tsconfig": "tsconfig.json"
    }]
  },
  "moduleNameMapper": {
    "^@recipe-manager/shared(.*)$": "<rootDir>/../packages/shared/src/index.ts"
  }
}
```

**apps/api/tests/app.e2e-spec.ts** (smoke test — wired to actual app in Plan 03):
```typescript
/**
 * Smoke test for API-03: Swagger UI is reachable at /api/docs.
 *
 * NOTE: This test imports AppModule which is created in Plan 03.
 * Until Plan 03 runs, this file exists as a scaffold.
 * The import will resolve once apps/api/src/app.module.ts is created.
 */
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication, ValidationPipe } from '@nestjs/common';
import { SwaggerModule, DocumentBuilder } from '@nestjs/swagger';
import * as request from 'supertest';
// AppModule is created in Plan 03
// import { AppModule } from '../src/app.module';

describe('API smoke test (e2e)', () => {
  let app: INestApplication;

  beforeAll(async () => {
    // Placeholder: will import AppModule after Plan 03 creates it.
    // For now, skip setup to allow the file to parse without errors.
  });

  afterAll(async () => {
    if (app) await app.close();
  });

  it('placeholder — wired to AppModule in Plan 03', () => {
    expect(true).toBe(true);
  });
});
```

The actual full e2e test (importing AppModule and testing GET /api/docs → 200) is completed in Plan 03 once AppModule exists.
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && test -f apps/api/jest.config.ts && echo "jest.config OK" && test -f apps/api/test/jest-e2e.json && echo "jest-e2e.json OK" && test -f apps/api/tests/app.e2e-spec.ts && echo "e2e spec OK"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/jest.config.ts exists and contains `moduleFileExtensions` and `moduleNameMapper` with `@recipe-manager/shared` mapping
    - apps/api/test/jest-e2e.json exists and contains `.e2e-spec.ts$` testRegex
    - apps/api/tests/app.e2e-spec.ts exists and contains `describe('API smoke test (e2e)'`
    - apps/api/test/jest-e2e.json contains `moduleNameMapper` with `@recipe-manager/shared` mapping
  </acceptance_criteria>
  <done>Wave 0 test infrastructure exists. The e2e spec scaffold is in place; it will be completed in Plan 03 once AppModule exists.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `yarn install` exits 0 from repo root
2. `cat package.json | grep workspaces` shows `"apps/*"` and `"packages/*"`
3. `cat .yarnrc.yml` shows `nodeLinker: node-modules`
4. `cat apps/api/package.json | grep @recipe-manager/shared` shows `"workspace:*"`
5. `cat apps/web/package.json | grep @recipe-manager/shared` shows `"workspace:*"`
6. `yarn workspace @recipe-manager/api prisma generate` exits 0
7. All three test infra files exist: `jest.config.ts`, `test/jest-e2e.json`, `tests/app.e2e-spec.ts`
</verification>

<success_criteria>
- `yarn install` succeeds with no errors
- All three workspace package.json files have correct `@recipe-manager/*` names
- Both apps have `@recipe-manager/shared: "workspace:*"` in dependencies
- Both apps have tsconfig.json paths pointing to `../../packages/shared/src/index.ts`
- `apps/api/prisma/schema.prisma` exists with datasource + generator (no models)
- Wave 0 test infra: `jest.config.ts`, `test/jest-e2e.json`, `tests/app.e2e-spec.ts` all exist
- No TypeScript or install errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-shared-types/01-01-SUMMARY.md`
</output>

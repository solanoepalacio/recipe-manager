---
phase: 17-sub-resource-commands
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/rmapi/tests/test_sections.py
  - tools/rmapi/tests/test_steps.py
  - tools/rmapi/rmapi/commands/sections.py
  - tools/rmapi/rmapi/commands/steps.py
  - tools/rmapi/rmapi/commands/ingredients.py
  - tools/rmapi/rmapi/cli.py
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, STP-01, STP-02, STP-03, STP-04]

must_haves:
  truths:
    - "rmapi sections add <recipe-id> --title X creates a section and returns its id in JSON"
    - "rmapi sections update <recipe-id> <section-id> --title X patches only the supplied field"
    - "rmapi sections delete <recipe-id> <section-id> --yes deletes and exits 0; without --yes exits 4"
    - "rmapi sections reorder <recipe-id> --ids id1,id2 sends ids list via PUT and emits {ok: true}"
    - "rmapi steps add <recipe-id> --body X creates a step and returns its id in JSON"
    - "rmapi steps update <recipe-id> <step-id> --body X patches only the supplied field"
    - "rmapi steps delete <recipe-id> <step-id> --yes deletes and exits 0; without --yes exits 4"
    - "rmapi steps reorder <recipe-id> --ids id1,id2 sends ids list via PUT and emits {ok: true}"
  artifacts:
    - path: "tools/rmapi/rmapi/commands/sections.py"
      provides: "4 Click subcommands: add, update, delete, reorder"
      contains: "def sections_add"
    - path: "tools/rmapi/rmapi/commands/steps.py"
      provides: "4 Click subcommands: add, update, delete, reorder"
      contains: "def steps_add"
    - path: "tools/rmapi/tests/test_sections.py"
      provides: "5+ tests covering SEC-01 through SEC-04"
      contains: "test_sections_add"
    - path: "tools/rmapi/tests/test_steps.py"
      provides: "5+ tests covering STP-01 through STP-04"
      contains: "test_steps_add"
    - path: "tools/rmapi/rmapi/cli.py"
      provides: "Registration of sections, ingredients, steps command groups"
      contains: "cli.add_command(sections)"
  key_links:
    - from: "tools/rmapi/rmapi/commands/sections.py"
      to: "POST /api/recipes/:id/sections"
      via: "http.post(f'/api/recipes/{recipe_id}/sections', body)"
      pattern: 'http\.post\(f"/api/recipes/\{recipe_id\}/sections"'
    - from: "tools/rmapi/rmapi/commands/sections.py"
      to: "PUT /api/recipes/:id/sections/reorder"
      via: "http.put(f'/api/recipes/{recipe_id}/sections/reorder', ...)"
      pattern: 'http\.put\(f"/api/recipes/\{recipe_id\}/sections/reorder"'
    - from: "tools/rmapi/rmapi/commands/steps.py"
      to: "POST /api/recipes/:id/steps"
      via: "http.post(f'/api/recipes/{recipe_id}/steps', body)"
      pattern: 'http\.post\(f"/api/recipes/\{recipe_id\}/steps"'
    - from: "tools/rmapi/rmapi/commands/steps.py"
      to: "PUT /api/recipes/:id/steps/reorder"
      via: "http.put(f'/api/recipes/{recipe_id}/steps/reorder', ...)"
      pattern: 'http\.put\(f"/api/recipes/\{recipe_id\}/steps/reorder"'
---

<objective>
Implement `rmapi sections` (add/update/delete/reorder) and `rmapi steps` (add/update/delete/reorder) using TDD, and register all three new command groups (sections, steps, ingredients) in cli.py.

Purpose: Enable the agent to manage ingredient sections and instruction steps on recipes via CLI. Also prepares cli.py registration and an ingredients stub for Plan 02.
Output: 8 working commands with 10+ passing tests across 2 new test files, plus cli.py updated with all 3 group registrations.
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sub-resource-commands/17-RESEARCH.md

<interfaces>
<!-- Existing modules the executor must use -->

From tools/rmapi/rmapi/http.py:
```python
def post(path: str, json_body: dict | None = None) -> dict | list:
def patch(path: str, json_body: dict | None = None) -> dict | list:
def delete(path: str) -> dict | None:
def put(path: str, json_body: dict | None = None) -> dict | list:
```

From tools/rmapi/rmapi/utils.py:
```python
def require_yes(yes: bool) -> None:
    # Exits with code 4 and JSON error on non-TTY without --yes
```

From tools/rmapi/rmapi/cli.py (current state):
```python
from .commands.recipes import recipes
from .commands.foods import foods
from .commands.units import units

cli.add_command(recipes)
cli.add_command(foods)
cli.add_command(units)
```

From tools/rmapi/tests/conftest.py:
```python
@pytest.fixture
def runner():
    return CliRunner()
```

API endpoint reference (from RESEARCH.md):
```
# Sections
POST   /api/recipes/:id/sections              -> 201, {id, recipeId, title, order}
PATCH  /api/recipes/:id/sections/:sectionId   -> 200, {id, recipeId, title, order}
DELETE /api/recipes/:id/sections/:sectionId   -> 200, {id: sectionId}
PUT    /api/recipes/:id/sections/reorder       -> 200, (void / empty)

# Steps
POST   /api/recipes/:id/steps              -> 201, {id, recipeId, title, body, order}
PATCH  /api/recipes/:id/steps/:stepId      -> 200, {id, recipeId, title, body, order}
DELETE /api/recipes/:id/steps/:stepId      -> 200, {id: stepId}
PUT    /api/recipes/:id/steps/reorder       -> 200, (void / empty)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for sections and steps commands, create stub command files, register all 3 groups in cli.py</name>
  <files>
    tools/rmapi/tests/test_sections.py,
    tools/rmapi/tests/test_steps.py,
    tools/rmapi/rmapi/commands/sections.py,
    tools/rmapi/rmapi/commands/steps.py,
    tools/rmapi/rmapi/commands/ingredients.py,
    tools/rmapi/rmapi/cli.py
  </files>
  <read_first>
    - tools/rmapi/tests/test_recipes.py
    - tools/rmapi/rmapi/commands/recipes.py
    - tools/rmapi/rmapi/cli.py
    - tools/rmapi/rmapi/http.py
    - tools/rmapi/rmapi/utils.py
    - tools/rmapi/tests/conftest.py
  </read_first>
  <behavior>
    Sections tests (test_sections.py):
    - test_sections_add: invoke ["sections", "add", "r1", "--title", "Sauce"] with mock POST returning FAKE_SECTION_RESPONSE -> exit 0, stdout JSON has "id" key
    - test_sections_add_no_title: invoke ["sections", "add", "r1"] with mock POST -> exit 0 (title is optional)
    - test_sections_update_title: invoke ["sections", "update", "r1", "sec1", "--title", "New"] with mock PATCH -> exit 0, body == {"title": "New"}
    - test_sections_delete_with_yes: invoke ["sections", "delete", "r1", "sec1", "--yes"] with mock DELETE returning {"id": "sec1"} -> exit 0, stdout JSON has id
    - test_sections_delete_requires_yes: invoke without --yes -> exit 4, output contains "confirmation_required"
    - test_sections_reorder: invoke ["sections", "reorder", "r1", "--ids", "sec2,sec1"] with mock PUT -> exit 0, body["ids"] == ["sec2", "sec1"], stdout is valid JSON

    Steps tests (test_steps.py):
    - test_steps_add: invoke ["steps", "add", "r1", "--body", "Boil water"] with mock POST returning FAKE_STEP_RESPONSE -> exit 0, stdout JSON has "id" key
    - test_steps_add_with_title: invoke ["steps", "add", "r1", "--body", "Boil", "--title", "Step 1"] -> body has both "body" and "title"
    - test_steps_update_body: invoke ["steps", "update", "r1", "stp1", "--body", "Simmer"] with mock PATCH -> exit 0, body == {"body": "Simmer"}
    - test_steps_update_title: invoke ["steps", "update", "r1", "stp1", "--title", "New Title"] -> body == {"title": "New Title"}
    - test_steps_delete_with_yes: invoke ["steps", "delete", "r1", "stp1", "--yes"] with mock DELETE -> exit 0
    - test_steps_delete_requires_yes: invoke without --yes -> exit 4
    - test_steps_reorder: invoke ["steps", "reorder", "r1", "--ids", "stp2,stp1"] with mock PUT -> exit 0, body["ids"] == ["stp2", "stp1"]
  </behavior>
  <action>
Create 5 new files and modify 1 existing file:

**1. tools/rmapi/tests/test_sections.py** -- new test file with 6 tests:

```python
"""Tests for sections commands -- SEC-01, SEC-02, SEC-03, SEC-04."""

import json
from unittest.mock import MagicMock, patch

from rmapi.cli import cli

ENV = {"RMAPI_BASE_URL": "http://localhost:3001", "RMAPI_TOKEN": "test-token"}

FAKE_SECTION_RESPONSE = {
    "id": "sec1",
    "recipeId": "r1",
    "title": "Sauce",
    "order": 0,
}


def _mock_post(payload, status=201):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload
    return mock_resp


def _mock_patch(payload, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload
    return mock_resp


def _mock_delete(payload=None, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload if payload is not None else {"id": "sec1"}
    return mock_resp


def _mock_put(payload=None, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload or {}
    return mock_resp


# SEC-01
def test_sections_add(runner):
    with patch("requests.post", return_value=_mock_post(FAKE_SECTION_RESPONSE)):
        result = runner.invoke(cli, ["sections", "add", "r1", "--title", "Sauce"], env=ENV)
    assert result.exit_code == 0
    data = json.loads(result.stdout)
    assert data["id"] == "sec1"


def test_sections_add_no_title(runner):
    with patch("requests.post", return_value=_mock_post(FAKE_SECTION_RESPONSE)) as mock_req:
        result = runner.invoke(cli, ["sections", "add", "r1"], env=ENV)
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body == {}


# SEC-02
def test_sections_update_title(runner):
    with patch("requests.patch", return_value=_mock_patch(FAKE_SECTION_RESPONSE)) as mock_req:
        result = runner.invoke(
            cli, ["sections", "update", "r1", "sec1", "--title", "New"], env=ENV
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body == {"title": "New"}


# SEC-03
def test_sections_delete_with_yes(runner):
    with patch("requests.delete", return_value=_mock_delete()):
        result = runner.invoke(cli, ["sections", "delete", "r1", "sec1", "--yes"], env=ENV)
    assert result.exit_code == 0
    data = json.loads(result.stdout)
    assert data["id"] == "sec1"


def test_sections_delete_requires_yes(runner):
    result = runner.invoke(cli, ["sections", "delete", "r1", "sec1"], env=ENV)
    assert result.exit_code == 4
    assert "confirmation_required" in result.output


# SEC-04
def test_sections_reorder(runner):
    with patch("requests.put", return_value=_mock_put()) as mock_req:
        result = runner.invoke(
            cli, ["sections", "reorder", "r1", "--ids", "sec2,sec1"], env=ENV
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body["ids"] == ["sec2", "sec1"]
    data = json.loads(result.stdout)
    assert data["ok"] is True
```

**2. tools/rmapi/tests/test_steps.py** -- new test file with 7 tests:

```python
"""Tests for steps commands -- STP-01, STP-02, STP-03, STP-04."""

import json
from unittest.mock import MagicMock, patch

from rmapi.cli import cli

ENV = {"RMAPI_BASE_URL": "http://localhost:3001", "RMAPI_TOKEN": "test-token"}

FAKE_STEP_RESPONSE = {
    "id": "stp1",
    "recipeId": "r1",
    "title": None,
    "body": "Boil water",
    "order": 0,
}


def _mock_post(payload, status=201):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload
    return mock_resp


def _mock_patch(payload, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload
    return mock_resp


def _mock_delete(payload=None, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload if payload is not None else {"id": "stp1"}
    return mock_resp


def _mock_put(payload=None, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload or {}
    return mock_resp


# STP-01
def test_steps_add(runner):
    with patch("requests.post", return_value=_mock_post(FAKE_STEP_RESPONSE)):
        result = runner.invoke(cli, ["steps", "add", "r1", "--body", "Boil water"], env=ENV)
    assert result.exit_code == 0
    data = json.loads(result.stdout)
    assert data["id"] == "stp1"


def test_steps_add_with_title(runner):
    with patch("requests.post", return_value=_mock_post(FAKE_STEP_RESPONSE)) as mock_req:
        result = runner.invoke(
            cli, ["steps", "add", "r1", "--body", "Boil", "--title", "Step 1"], env=ENV
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body["body"] == "Boil"
    assert body["title"] == "Step 1"


# STP-02
def test_steps_update_body(runner):
    with patch("requests.patch", return_value=_mock_patch(FAKE_STEP_RESPONSE)) as mock_req:
        result = runner.invoke(
            cli, ["steps", "update", "r1", "stp1", "--body", "Simmer"], env=ENV
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body == {"body": "Simmer"}


def test_steps_update_title(runner):
    with patch("requests.patch", return_value=_mock_patch(FAKE_STEP_RESPONSE)) as mock_req:
        result = runner.invoke(
            cli, ["steps", "update", "r1", "stp1", "--title", "New Title"], env=ENV
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body == {"title": "New Title"}


# STP-03
def test_steps_delete_with_yes(runner):
    with patch("requests.delete", return_value=_mock_delete()):
        result = runner.invoke(cli, ["steps", "delete", "r1", "stp1", "--yes"], env=ENV)
    assert result.exit_code == 0
    data = json.loads(result.stdout)
    assert data["id"] == "stp1"


def test_steps_delete_requires_yes(runner):
    result = runner.invoke(cli, ["steps", "delete", "r1", "stp1"], env=ENV)
    assert result.exit_code == 4
    assert "confirmation_required" in result.output


# STP-04
def test_steps_reorder(runner):
    with patch("requests.put", return_value=_mock_put()) as mock_req:
        result = runner.invoke(
            cli, ["steps", "reorder", "r1", "--ids", "stp2,stp1"], env=ENV
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body["ids"] == ["stp2", "stp1"]
    data = json.loads(result.stdout)
    assert data["ok"] is True
```

**3. tools/rmapi/rmapi/commands/sections.py** -- stub file so tests can import without ImportError:

```python
"""Ingredient section commands."""

import click


@click.group()
def sections() -> None:
    """Ingredient section commands."""
    pass
```

**4. tools/rmapi/rmapi/commands/steps.py** -- stub file:

```python
"""Instruction step commands."""

import click


@click.group()
def steps() -> None:
    """Instruction step commands."""
    pass
```

**5. tools/rmapi/rmapi/commands/ingredients.py** -- stub file for Plan 02:

```python
"""Ingredient commands."""

import click


@click.group()
def ingredients() -> None:
    """Ingredient commands."""
    pass
```

**6. tools/rmapi/rmapi/cli.py** -- add 3 new imports and 3 new `cli.add_command` calls after the existing ones:

Add after `from .commands.units import units`:
```python
from .commands.sections import sections
from .commands.ingredients import ingredients
from .commands.steps import steps
```

Add after `cli.add_command(units)`:
```python
cli.add_command(sections)
cli.add_command(ingredients)
cli.add_command(steps)
```

Run tests after creation -- all 13 new tests (6 sections + 7 steps) should FAIL (commands are stubs). Existing tests should still PASS.
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/test_sections.py tools/rmapi/tests/test_steps.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tools/rmapi/tests/test_sections.py contains def test_sections_add
    - tools/rmapi/tests/test_sections.py contains def test_sections_add_no_title
    - tools/rmapi/tests/test_sections.py contains def test_sections_update_title
    - tools/rmapi/tests/test_sections.py contains def test_sections_delete_with_yes
    - tools/rmapi/tests/test_sections.py contains def test_sections_delete_requires_yes
    - tools/rmapi/tests/test_sections.py contains def test_sections_reorder
    - tools/rmapi/tests/test_sections.py contains FAKE_SECTION_RESPONSE
    - tools/rmapi/tests/test_steps.py contains def test_steps_add
    - tools/rmapi/tests/test_steps.py contains def test_steps_add_with_title
    - tools/rmapi/tests/test_steps.py contains def test_steps_update_body
    - tools/rmapi/tests/test_steps.py contains def test_steps_update_title
    - tools/rmapi/tests/test_steps.py contains def test_steps_delete_with_yes
    - tools/rmapi/tests/test_steps.py contains def test_steps_delete_requires_yes
    - tools/rmapi/tests/test_steps.py contains def test_steps_reorder
    - tools/rmapi/tests/test_steps.py contains FAKE_STEP_RESPONSE
    - tools/rmapi/rmapi/commands/sections.py contains def sections()
    - tools/rmapi/rmapi/commands/steps.py contains def steps()
    - tools/rmapi/rmapi/commands/ingredients.py contains def ingredients()
    - tools/rmapi/rmapi/cli.py contains from .commands.sections import sections
    - tools/rmapi/rmapi/cli.py contains from .commands.ingredients import ingredients
    - tools/rmapi/rmapi/cli.py contains from .commands.steps import steps
    - tools/rmapi/rmapi/cli.py contains cli.add_command(sections)
    - tools/rmapi/rmapi/cli.py contains cli.add_command(ingredients)
    - tools/rmapi/rmapi/cli.py contains cli.add_command(steps)
    - The 13 new tests FAIL (RED phase -- commands are stubs)
    - Existing test suite (test_recipes.py, test_foods.py, test_units.py) still passes
  </acceptance_criteria>
  <done>13 failing test functions across test_sections.py (6) and test_steps.py (7); 3 stub command files created; cli.py registers all 3 new groups; existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sections and steps commands to make all tests pass</name>
  <files>
    tools/rmapi/rmapi/commands/sections.py,
    tools/rmapi/rmapi/commands/steps.py
  </files>
  <read_first>
    - tools/rmapi/rmapi/commands/sections.py
    - tools/rmapi/rmapi/commands/steps.py
    - tools/rmapi/tests/test_sections.py
    - tools/rmapi/tests/test_steps.py
    - tools/rmapi/rmapi/commands/recipes.py
    - tools/rmapi/rmapi/http.py
    - tools/rmapi/rmapi/utils.py
  </read_first>
  <behavior>
    sections.py:
    - sections_add: argument recipe_id, optional --title, sparse body, http.post(f"/api/recipes/{recipe_id}/sections", body), echo JSON
    - sections_update: arguments recipe_id + section_id, optional --title, sparse body, http.patch(f"/api/recipes/{recipe_id}/sections/{section_id}", body), echo JSON
    - sections_delete: arguments recipe_id + section_id, --yes flag, require_yes(yes), http.delete(f"/api/recipes/{recipe_id}/sections/{section_id}"), echo JSON
    - sections_reorder: argument recipe_id, required --ids, split comma string to list, http.put(f"/api/recipes/{recipe_id}/sections/reorder", {"ids": ids_list}), echo {"ok": true}

    steps.py:
    - steps_add: argument recipe_id, required --body, optional --title, sparse body, http.post(f"/api/recipes/{recipe_id}/steps", body), echo JSON
    - steps_update: arguments recipe_id + step_id, optional --body and --title, sparse body, http.patch(f"/api/recipes/{recipe_id}/steps/{step_id}", body), echo JSON
    - steps_delete: arguments recipe_id + step_id, --yes flag, require_yes(yes), http.delete(f"/api/recipes/{recipe_id}/steps/{step_id}"), echo JSON
    - steps_reorder: argument recipe_id, required --ids, split to list, http.put(f"/api/recipes/{recipe_id}/steps/reorder", {"ids": ids_list}), echo {"ok": true}
  </behavior>
  <action>
Replace the stub content in both files with full implementations.

**tools/rmapi/rmapi/commands/sections.py** -- full implementation:

```python
"""Ingredient section commands."""

import json

import click

from .. import http
from ..utils import require_yes


@click.group()
def sections() -> None:
    """Ingredient section commands."""
    pass


@sections.command("add")
@click.argument("recipe_id")
@click.option("--title", default=None, help="Section title")
def sections_add(recipe_id, title):
    """Add an ingredient section to a recipe."""
    body = {k: v for k, v in {"title": title}.items() if v is not None}
    data = http.post(f"/api/recipes/{recipe_id}/sections", body)
    click.echo(json.dumps(data))


@sections.command("update")
@click.argument("recipe_id")
@click.argument("section_id")
@click.option("--title", default=None, help="New section title")
def sections_update(recipe_id, section_id, title):
    """Update a section title."""
    body = {k: v for k, v in {"title": title}.items() if v is not None}
    data = http.patch(f"/api/recipes/{recipe_id}/sections/{section_id}", body)
    click.echo(json.dumps(data))


@sections.command("delete")
@click.argument("recipe_id")
@click.argument("section_id")
@click.option("--yes", is_flag=True, default=False, help="Skip confirmation prompt")
def sections_delete(recipe_id, section_id, yes):
    """Delete an ingredient section."""
    require_yes(yes)
    result = http.delete(f"/api/recipes/{recipe_id}/sections/{section_id}")
    click.echo(json.dumps(result))


@sections.command("reorder")
@click.argument("recipe_id")
@click.option("--ids", required=True, help="Comma-separated section IDs in desired order")
def sections_reorder(recipe_id, ids):
    """Reorder sections by specifying all IDs in desired order."""
    ids_list = [i.strip() for i in ids.split(",") if i.strip()]
    http.put(f"/api/recipes/{recipe_id}/sections/reorder", {"ids": ids_list})
    click.echo(json.dumps({"ok": True}))
```

**tools/rmapi/rmapi/commands/steps.py** -- full implementation:

```python
"""Instruction step commands."""

import json

import click

from .. import http
from ..utils import require_yes


@click.group()
def steps() -> None:
    """Instruction step commands."""
    pass


@steps.command("add")
@click.argument("recipe_id")
@click.option("--body", "step_body", required=True, help="Step instruction text")
@click.option("--title", default=None, help="Optional step title")
def steps_add(recipe_id, step_body, title):
    """Add an instruction step to a recipe."""
    body = {k: v for k, v in {"body": step_body, "title": title}.items() if v is not None}
    data = http.post(f"/api/recipes/{recipe_id}/steps", body)
    click.echo(json.dumps(data))


@steps.command("update")
@click.argument("recipe_id")
@click.argument("step_id")
@click.option("--body", "step_body", default=None, help="New step instruction text")
@click.option("--title", default=None, help="New step title")
def steps_update(recipe_id, step_id, step_body, title):
    """Update an instruction step (only supplied fields are changed)."""
    body = {k: v for k, v in {"body": step_body, "title": title}.items() if v is not None}
    data = http.patch(f"/api/recipes/{recipe_id}/steps/{step_id}", body)
    click.echo(json.dumps(data))


@steps.command("delete")
@click.argument("recipe_id")
@click.argument("step_id")
@click.option("--yes", is_flag=True, default=False, help="Skip confirmation prompt")
def steps_delete(recipe_id, step_id, yes):
    """Delete an instruction step."""
    require_yes(yes)
    result = http.delete(f"/api/recipes/{recipe_id}/steps/{step_id}")
    click.echo(json.dumps(result))


@steps.command("reorder")
@click.argument("recipe_id")
@click.option("--ids", required=True, help="Comma-separated step IDs in desired order")
def steps_reorder(recipe_id, ids):
    """Reorder steps by specifying all IDs in desired order."""
    ids_list = [i.strip() for i in ids.split(",") if i.strip()]
    http.put(f"/api/recipes/{recipe_id}/steps/reorder", {"ids": ids_list})
    click.echo(json.dumps({"ok": True}))
```

CRITICAL implementation notes:
- `steps add` uses `"step_body"` as the Python parameter name (via `@click.option("--body", "step_body", ...)`) to avoid shadowing the `body` dict variable used in the sparse comprehension
- Reorder commands call `http.put` (not `http.patch` or `http.post`) -- the backend uses PUT for reorder endpoints
- Reorder commands emit `{"ok": True}` unconditionally because the backend returns empty body on reorder
- Delete commands call `require_yes(yes)` BEFORE any HTTP call
- Sparse body `{k: v for k, v in {...}.items() if v is not None}` filters out unset optional fields
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/test_sections.py tools/rmapi/tests/test_steps.py -v && .venv/bin/pytest tools/rmapi/tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - tools/rmapi/rmapi/commands/sections.py contains def sections_add(
    - tools/rmapi/rmapi/commands/sections.py contains def sections_update(
    - tools/rmapi/rmapi/commands/sections.py contains def sections_delete(
    - tools/rmapi/rmapi/commands/sections.py contains def sections_reorder(
    - tools/rmapi/rmapi/commands/sections.py contains http.post(f"/api/recipes/{recipe_id}/sections"
    - tools/rmapi/rmapi/commands/sections.py contains http.patch(f"/api/recipes/{recipe_id}/sections/{section_id}"
    - tools/rmapi/rmapi/commands/sections.py contains http.delete(f"/api/recipes/{recipe_id}/sections/{section_id}"
    - tools/rmapi/rmapi/commands/sections.py contains http.put(f"/api/recipes/{recipe_id}/sections/reorder"
    - tools/rmapi/rmapi/commands/sections.py contains require_yes(yes)
    - tools/rmapi/rmapi/commands/steps.py contains def steps_add(
    - tools/rmapi/rmapi/commands/steps.py contains def steps_update(
    - tools/rmapi/rmapi/commands/steps.py contains def steps_delete(
    - tools/rmapi/rmapi/commands/steps.py contains def steps_reorder(
    - tools/rmapi/rmapi/commands/steps.py contains http.post(f"/api/recipes/{recipe_id}/steps"
    - tools/rmapi/rmapi/commands/steps.py contains http.patch(f"/api/recipes/{recipe_id}/steps/{step_id}"
    - tools/rmapi/rmapi/commands/steps.py contains http.delete(f"/api/recipes/{recipe_id}/steps/{step_id}"
    - tools/rmapi/rmapi/commands/steps.py contains http.put(f"/api/recipes/{recipe_id}/steps/reorder"
    - tools/rmapi/rmapi/commands/steps.py contains require_yes(yes)
    - tools/rmapi/rmapi/commands/steps.py contains "step_body" (renamed param to avoid body variable collision)
    - All 13 new tests pass (GREEN phase)
    - Full rmapi test suite exits 0
  </acceptance_criteria>
  <done>All 13 sections+steps tests pass (GREEN); full rmapi test suite green; sections has 4 commands (add/update/delete/reorder); steps has 4 commands (add/update/delete/reorder); all use sparse body, require_yes for delete, http.put for reorder</done>
</task>

</tasks>

<verification>
1. `cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/test_sections.py -v` -- 6 tests pass
2. `cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/test_steps.py -v` -- 7 tests pass
3. `cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/ -v` -- full suite passes
4. `grep -c "def test_" tools/rmapi/tests/test_sections.py` -- outputs 6
5. `grep -c "def test_" tools/rmapi/tests/test_steps.py` -- outputs 7
6. `grep "require_yes" tools/rmapi/rmapi/commands/sections.py` -- confirms delete guard
7. `grep "require_yes" tools/rmapi/rmapi/commands/steps.py` -- confirms delete guard
8. `grep "http.put" tools/rmapi/rmapi/commands/sections.py` -- confirms PUT for reorder
9. `grep "http.put" tools/rmapi/rmapi/commands/steps.py` -- confirms PUT for reorder
</verification>

<success_criteria>
- `rmapi sections add <recipe-id> --title X` creates a section and returns id JSON
- `rmapi sections update/delete/reorder` all work with correct HTTP verbs
- `rmapi steps add <recipe-id> --body X` creates a step and returns id JSON
- `rmapi steps update/delete/reorder` all work with correct HTTP verbs
- Delete commands require --yes on non-TTY (exit 4 without)
- Reorder commands emit {"ok": true} after PUT
- 13 total new tests across 2 files, all passing
- Full rmapi test suite green
- cli.py registers sections, ingredients, steps groups
</success_criteria>

<output>
After completion, create `.planning/phases/17-sub-resource-commands/17-01-SUMMARY.md`
</output>

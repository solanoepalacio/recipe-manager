---
phase: 17-sub-resource-commands
plan: "02"
type: tdd
wave: 2
depends_on: ["17-01"]
files_modified:
  - tools/rmapi/tests/test_ingredients.py
  - tools/rmapi/rmapi/commands/ingredients.py
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-04]

must_haves:
  truths:
    - "rmapi ingredients add <recipe-id> <section-id> --food-id X creates an ingredient and returns its id in JSON"
    - "rmapi ingredients add requires --food-id (Click enforces required=True)"
    - "rmapi ingredients update <recipe-id> <section-id> <ingredient-id> patches only supplied fields"
    - "rmapi ingredients delete <recipe-id> <section-id> <ingredient-id> --yes deletes and exits 0; without --yes exits 4"
    - "rmapi ingredients reorder <recipe-id> <section-id> --ids id1,id2 sends ids list via PUT to the 3-level nested path"
  artifacts:
    - path: "tools/rmapi/rmapi/commands/ingredients.py"
      provides: "4 Click subcommands: add, update, delete, reorder"
      contains: "def ingredients_add"
    - path: "tools/rmapi/tests/test_ingredients.py"
      provides: "7+ tests covering ING-01 through ING-04"
      contains: "test_ingredients_add"
  key_links:
    - from: "tools/rmapi/rmapi/commands/ingredients.py"
      to: "POST /api/recipes/:id/sections/:sectionId/ingredients"
      via: "http.post(f'/api/recipes/{recipe_id}/sections/{section_id}/ingredients', body)"
      pattern: 'http\.post\(f"/api/recipes/\{recipe_id\}/sections/\{section_id\}/ingredients"'
    - from: "tools/rmapi/rmapi/commands/ingredients.py"
      to: "PUT /api/recipes/:id/sections/:sectionId/ingredients/reorder"
      via: "http.put with 3-level nested path"
      pattern: 'http\.put\(f"/api/recipes/\{recipe_id\}/sections/\{section_id\}/ingredients/reorder"'
---

<objective>
Implement `rmapi ingredients` (add/update/delete/reorder) using TDD -- completing the full sub-resource command set for recipe ingredient management.

Purpose: Enable the agent to add, edit, remove, and reorder ingredients within recipe sections via CLI. Ingredients have 3-level nesting (recipe -> section -> ingredient), making them the most complex sub-resource.
Output: 4 working commands with 7+ passing tests.
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sub-resource-commands/17-RESEARCH.md
@.planning/phases/17-sub-resource-commands/17-01-SUMMARY.md

<interfaces>
<!-- Existing modules the executor must use -->

From tools/rmapi/rmapi/http.py:
```python
def post(path: str, json_body: dict | None = None) -> dict | list:
def patch(path: str, json_body: dict | None = None) -> dict | list:
def delete(path: str) -> dict | None:
def put(path: str, json_body: dict | None = None) -> dict | list:
```

From tools/rmapi/rmapi/utils.py:
```python
def require_yes(yes: bool) -> None:
    # Exits with code 4 and JSON error on non-TTY without --yes
```

From tools/rmapi/rmapi/commands/ingredients.py (after Plan 01 -- stub):
```python
@click.group()
def ingredients() -> None:
    """Ingredient commands."""
    pass
```

From tools/rmapi/rmapi/cli.py (after Plan 01):
```python
# Already registered: cli.add_command(ingredients)
```

API endpoint reference (from RESEARCH.md):
```
# Ingredients (3-level nesting: recipe -> section -> ingredient)
POST   /api/recipes/:id/sections/:sectionId/ingredients                       -> 201, {id, sectionId, foodId, food:{id,name}, unitId, unit:{id,name,abbreviation}, quantity, note, order}
PATCH  /api/recipes/:id/sections/:sectionId/ingredients/:ingredientId        -> 200, same shape
DELETE /api/recipes/:id/sections/:sectionId/ingredients/:ingredientId        -> 200, {id: ingredientId}
PUT    /api/recipes/:id/sections/:sectionId/ingredients/reorder              -> 200, (void / empty)
```

Request bodies:
```python
# ingredients add (foodId required, rest optional)
body = {"foodId": "f1", "unitId": "u1", "quantity": 200.0, "note": "chopped"}
# ingredients update (all optional, sparse)
body = {"quantity": 100.0}
# ingredients reorder
body = {"ids": ["ing2", "ing1"]}
```

CRITICAL: Ingredient responses include nested `food` and `unit` objects:
```python
{
    "id": "ing1",
    "sectionId": "sec1",
    "foodId": "f1",
    "food": {"id": "f1", "name": "tomato"},
    "unitId": "u1",
    "unit": {"id": "u1", "name": "gram", "abbreviation": "g"},
    "quantity": 200.0,
    "note": None,
    "order": 0,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for ingredients add, update, delete, and reorder</name>
  <files>tools/rmapi/tests/test_ingredients.py</files>
  <read_first>
    - tools/rmapi/tests/test_sections.py
    - tools/rmapi/tests/test_recipes.py
    - tools/rmapi/rmapi/commands/ingredients.py
    - tools/rmapi/rmapi/http.py
    - tools/rmapi/rmapi/utils.py
    - tools/rmapi/tests/conftest.py
  </read_first>
  <behavior>
    - test_ingredients_add: invoke ["ingredients", "add", "r1", "sec1", "--food-id", "f1", "--quantity", "200", "--unit-id", "u1"] with mock POST returning FAKE_INGREDIENT_RESPONSE -> exit 0, stdout JSON has "id" key
    - test_ingredients_add_with_note: invoke with --note "chopped" -> body includes "note": "chopped"
    - test_ingredients_add_food_id_required: invoke ["ingredients", "add", "r1", "sec1"] without --food-id -> exit != 0 (Click enforces required)
    - test_ingredients_update_quantity: invoke ["ingredients", "update", "r1", "sec1", "ing1", "--quantity", "100"] with mock PATCH -> exit 0, body == {"quantity": 100.0}
    - test_ingredients_update_sparse: invoke with only --note "diced" -> body == {"note": "diced"}
    - test_ingredients_delete_with_yes: invoke ["ingredients", "delete", "r1", "sec1", "ing1", "--yes"] with mock DELETE -> exit 0, stdout JSON has id
    - test_ingredients_delete_requires_yes: invoke without --yes -> exit 4, output contains "confirmation_required"
    - test_ingredients_reorder: invoke ["ingredients", "reorder", "r1", "sec1", "--ids", "ing2,ing1"] with mock PUT -> exit 0, body["ids"] == ["ing2", "ing1"]
  </behavior>
  <action>
Create new file `tools/rmapi/tests/test_ingredients.py` with 8 test functions and supporting constants/helpers.

```python
"""Tests for ingredients commands -- ING-01, ING-02, ING-03, ING-04."""

import json
from unittest.mock import MagicMock, patch

from rmapi.cli import cli

ENV = {"RMAPI_BASE_URL": "http://localhost:3001", "RMAPI_TOKEN": "test-token"}

FAKE_INGREDIENT_RESPONSE = {
    "id": "ing1",
    "sectionId": "sec1",
    "foodId": "f1",
    "food": {"id": "f1", "name": "tomato"},
    "unitId": "u1",
    "unit": {"id": "u1", "name": "gram", "abbreviation": "g"},
    "quantity": 200.0,
    "note": None,
    "order": 0,
}


def _mock_post(payload, status=201):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload
    return mock_resp


def _mock_patch(payload, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload
    return mock_resp


def _mock_delete(payload=None, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload if payload is not None else {"id": "ing1"}
    return mock_resp


def _mock_put(payload=None, status=200):
    mock_resp = MagicMock()
    mock_resp.status_code = status
    mock_resp.json.return_value = payload or {}
    return mock_resp


# ING-01
def test_ingredients_add(runner):
    with patch("requests.post", return_value=_mock_post(FAKE_INGREDIENT_RESPONSE)):
        result = runner.invoke(
            cli,
            ["ingredients", "add", "r1", "sec1", "--food-id", "f1",
             "--quantity", "200", "--unit-id", "u1"],
            env=ENV,
        )
    assert result.exit_code == 0
    data = json.loads(result.stdout)
    assert data["id"] == "ing1"


def test_ingredients_add_with_note(runner):
    with patch("requests.post", return_value=_mock_post(FAKE_INGREDIENT_RESPONSE)) as mock_req:
        result = runner.invoke(
            cli,
            ["ingredients", "add", "r1", "sec1", "--food-id", "f1",
             "--note", "chopped"],
            env=ENV,
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body["note"] == "chopped"
    assert body["foodId"] == "f1"


def test_ingredients_add_food_id_required(runner):
    """--food-id is required; Click should reject the command without it."""
    result = runner.invoke(cli, ["ingredients", "add", "r1", "sec1"], env=ENV)
    assert result.exit_code != 0


# ING-02
def test_ingredients_update_quantity(runner):
    with patch("requests.patch", return_value=_mock_patch(FAKE_INGREDIENT_RESPONSE)) as mock_req:
        result = runner.invoke(
            cli,
            ["ingredients", "update", "r1", "sec1", "ing1", "--quantity", "100"],
            env=ENV,
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body == {"quantity": 100.0}


def test_ingredients_update_sparse(runner):
    with patch("requests.patch", return_value=_mock_patch(FAKE_INGREDIENT_RESPONSE)) as mock_req:
        result = runner.invoke(
            cli,
            ["ingredients", "update", "r1", "sec1", "ing1", "--note", "diced"],
            env=ENV,
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body == {"note": "diced"}


# ING-03
def test_ingredients_delete_with_yes(runner):
    with patch("requests.delete", return_value=_mock_delete()):
        result = runner.invoke(
            cli, ["ingredients", "delete", "r1", "sec1", "ing1", "--yes"], env=ENV
        )
    assert result.exit_code == 0
    data = json.loads(result.stdout)
    assert data["id"] == "ing1"


def test_ingredients_delete_requires_yes(runner):
    result = runner.invoke(
        cli, ["ingredients", "delete", "r1", "sec1", "ing1"], env=ENV
    )
    assert result.exit_code == 4
    assert "confirmation_required" in result.output


# ING-04
def test_ingredients_reorder(runner):
    with patch("requests.put", return_value=_mock_put()) as mock_req:
        result = runner.invoke(
            cli, ["ingredients", "reorder", "r1", "sec1", "--ids", "ing2,ing1"], env=ENV
        )
    assert result.exit_code == 0
    body = mock_req.call_args.kwargs["json"]
    assert body["ids"] == ["ing2", "ing1"]
    data = json.loads(result.stdout)
    assert data["ok"] is True
```

Run tests after creation -- all 8 new tests should FAIL (ingredients stub has no subcommands). Existing tests should still PASS.
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/test_ingredients.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tools/rmapi/tests/test_ingredients.py contains def test_ingredients_add
    - tools/rmapi/tests/test_ingredients.py contains def test_ingredients_add_with_note
    - tools/rmapi/tests/test_ingredients.py contains def test_ingredients_add_food_id_required
    - tools/rmapi/tests/test_ingredients.py contains def test_ingredients_update_quantity
    - tools/rmapi/tests/test_ingredients.py contains def test_ingredients_update_sparse
    - tools/rmapi/tests/test_ingredients.py contains def test_ingredients_delete_with_yes
    - tools/rmapi/tests/test_ingredients.py contains def test_ingredients_delete_requires_yes
    - tools/rmapi/tests/test_ingredients.py contains def test_ingredients_reorder
    - tools/rmapi/tests/test_ingredients.py contains FAKE_INGREDIENT_RESPONSE
    - tools/rmapi/tests/test_ingredients.py contains "food": {"id": "f1", "name": "tomato"}
    - The 8 new tests FAIL (RED phase -- ingredients stub has no subcommands)
    - Existing test suite (test_recipes, test_sections, test_steps, test_foods, test_units) still passes
  </acceptance_criteria>
  <done>8 failing test functions in test_ingredients.py covering ING-01 through ING-04; FAKE_INGREDIENT_RESPONSE includes nested food and unit objects; existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ingredients add, update, delete, and reorder commands</name>
  <files>tools/rmapi/rmapi/commands/ingredients.py</files>
  <read_first>
    - tools/rmapi/rmapi/commands/ingredients.py
    - tools/rmapi/tests/test_ingredients.py
    - tools/rmapi/rmapi/commands/sections.py
    - tools/rmapi/rmapi/commands/recipes.py
    - tools/rmapi/rmapi/http.py
    - tools/rmapi/rmapi/utils.py
  </read_first>
  <behavior>
    - ingredients_add: arguments recipe_id + section_id, required --food-id, optional --quantity (float), --unit-id, --note; sparse body, http.post to 3-level nested path, echo JSON
    - ingredients_update: arguments recipe_id + section_id + ingredient_id, optional --food-id, --quantity (float), --unit-id, --note; sparse body, http.patch to 3-level nested path, echo JSON
    - ingredients_delete: arguments recipe_id + section_id + ingredient_id, --yes flag, require_yes, http.delete from 3-level nested path, echo JSON
    - ingredients_reorder: arguments recipe_id + section_id, required --ids, split to list, http.put to 3-level nested path, echo {"ok": true}
  </behavior>
  <action>
Replace the stub content in `tools/rmapi/rmapi/commands/ingredients.py` with the full implementation:

```python
"""Ingredient commands."""

import json

import click

from .. import http
from ..utils import require_yes


@click.group()
def ingredients() -> None:
    """Ingredient commands."""
    pass


@ingredients.command("add")
@click.argument("recipe_id")
@click.argument("section_id")
@click.option("--food-id", required=True, help="Food ID (use 'rmapi foods lookup' to find)")
@click.option("--quantity", default=None, type=float, help="Quantity amount")
@click.option("--unit-id", default=None, help="Unit ID (use 'rmapi units list' to find)")
@click.option("--note", default=None, help="Optional note (e.g. chopped, diced)")
def ingredients_add(recipe_id, section_id, food_id, quantity, unit_id, note):
    """Add an ingredient to a section."""
    body = {k: v for k, v in {
        "foodId": food_id,
        "quantity": quantity,
        "unitId": unit_id,
        "note": note,
    }.items() if v is not None}
    data = http.post(
        f"/api/recipes/{recipe_id}/sections/{section_id}/ingredients", body
    )
    click.echo(json.dumps(data))


@ingredients.command("update")
@click.argument("recipe_id")
@click.argument("section_id")
@click.argument("ingredient_id")
@click.option("--food-id", default=None, help="New food ID")
@click.option("--quantity", default=None, type=float, help="New quantity")
@click.option("--unit-id", default=None, help="New unit ID")
@click.option("--note", default=None, help="New note")
def ingredients_update(recipe_id, section_id, ingredient_id, food_id, quantity, unit_id, note):
    """Update an ingredient (only supplied fields are changed)."""
    body = {k: v for k, v in {
        "foodId": food_id,
        "quantity": quantity,
        "unitId": unit_id,
        "note": note,
    }.items() if v is not None}
    data = http.patch(
        f"/api/recipes/{recipe_id}/sections/{section_id}/ingredients/{ingredient_id}",
        body,
    )
    click.echo(json.dumps(data))


@ingredients.command("delete")
@click.argument("recipe_id")
@click.argument("section_id")
@click.argument("ingredient_id")
@click.option("--yes", is_flag=True, default=False, help="Skip confirmation prompt")
def ingredients_delete(recipe_id, section_id, ingredient_id, yes):
    """Delete an ingredient."""
    require_yes(yes)
    result = http.delete(
        f"/api/recipes/{recipe_id}/sections/{section_id}/ingredients/{ingredient_id}"
    )
    click.echo(json.dumps(result))


@ingredients.command("reorder")
@click.argument("recipe_id")
@click.argument("section_id")
@click.option("--ids", required=True, help="Comma-separated ingredient IDs in desired order")
def ingredients_reorder(recipe_id, section_id, ids):
    """Reorder ingredients within a section by specifying all IDs in desired order."""
    ids_list = [i.strip() for i in ids.split(",") if i.strip()]
    http.put(
        f"/api/recipes/{recipe_id}/sections/{section_id}/ingredients/reorder",
        {"ids": ids_list},
    )
    click.echo(json.dumps({"ok": True}))
```

CRITICAL implementation notes:
- `--food-id` on `add` is `required=True` -- the backend CreateIngredientDto.foodId is not optional
- `--quantity` uses `type=float` (not int) -- quantities can be decimal (e.g., 1.5)
- All paths use 3-level nesting: `/api/recipes/{recipe_id}/sections/{section_id}/ingredients[/{ingredient_id}]`
- Reorder path includes section_id: `/api/recipes/{recipe_id}/sections/{section_id}/ingredients/reorder`
- Sparse body pattern filters None values, same as recipes and sections
- `require_yes(yes)` called before HTTP delete, same pattern as sections/steps/recipes
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/test_ingredients.py -v && .venv/bin/pytest tools/rmapi/tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - tools/rmapi/rmapi/commands/ingredients.py contains def ingredients_add(
    - tools/rmapi/rmapi/commands/ingredients.py contains def ingredients_update(
    - tools/rmapi/rmapi/commands/ingredients.py contains def ingredients_delete(
    - tools/rmapi/rmapi/commands/ingredients.py contains def ingredients_reorder(
    - tools/rmapi/rmapi/commands/ingredients.py contains --food-id", required=True
    - tools/rmapi/rmapi/commands/ingredients.py contains type=float
    - tools/rmapi/rmapi/commands/ingredients.py contains http.post(
    - tools/rmapi/rmapi/commands/ingredients.py contains f"/api/recipes/{recipe_id}/sections/{section_id}/ingredients"
    - tools/rmapi/rmapi/commands/ingredients.py contains http.patch(
    - tools/rmapi/rmapi/commands/ingredients.py contains f"/api/recipes/{recipe_id}/sections/{section_id}/ingredients/{ingredient_id}"
    - tools/rmapi/rmapi/commands/ingredients.py contains http.delete(
    - tools/rmapi/rmapi/commands/ingredients.py contains http.put(
    - tools/rmapi/rmapi/commands/ingredients.py contains f"/api/recipes/{recipe_id}/sections/{section_id}/ingredients/reorder"
    - tools/rmapi/rmapi/commands/ingredients.py contains require_yes(yes)
    - All 8 ingredients tests pass (GREEN phase)
    - Full rmapi test suite exits 0 (all test files: test_recipes, test_sections, test_steps, test_ingredients, test_foods, test_units)
  </acceptance_criteria>
  <done>All 8 ingredients tests pass (GREEN); full rmapi test suite green; ingredients has 4 commands (add/update/delete/reorder); 3-level nested paths correct; food-id required on add; quantity is float type; sparse body filters None; delete guarded by require_yes; reorder uses http.put</done>
</task>

</tasks>

<verification>
1. `cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/test_ingredients.py -v` -- 8 tests pass
2. `cd /home/solanoe/code/recipe-manager && .venv/bin/pytest tools/rmapi/tests/ -v` -- full suite passes
3. `grep -c "def test_" tools/rmapi/tests/test_ingredients.py` -- outputs 8
4. `grep "require_yes" tools/rmapi/rmapi/commands/ingredients.py` -- confirms delete guard
5. `grep "http.put" tools/rmapi/rmapi/commands/ingredients.py` -- confirms PUT for reorder
6. `grep "required=True" tools/rmapi/rmapi/commands/ingredients.py` -- confirms food-id is required
7. `grep "section_id" tools/rmapi/rmapi/commands/ingredients.py` -- confirms 3-level nesting in all paths
</verification>

<success_criteria>
- `rmapi ingredients add <recipe-id> <section-id> --food-id X` creates an ingredient (food-id is required)
- `rmapi ingredients update <recipe-id> <section-id> <ingredient-id>` patches only supplied fields
- `rmapi ingredients delete <recipe-id> <section-id> <ingredient-id> --yes` deletes; without --yes exits 4
- `rmapi ingredients reorder <recipe-id> <section-id> --ids id1,id2` sends ids list via PUT to 3-level nested path
- All paths correctly use 3-level nesting: /api/recipes/:id/sections/:sectionId/ingredients[/:ingredientId]
- 8 total tests in test_ingredients.py, all passing
- Full rmapi test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/17-sub-resource-commands/17-02-SUMMARY.md`
</output>

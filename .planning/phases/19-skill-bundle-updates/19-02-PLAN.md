---
phase: 19-skill-bundle-updates
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/recipe-manager/recipes_get.md
  - skills/recipe-manager/recipes_edit.md
autonomous: true
requirements: [SKILL-11, SKILL-12]

must_haves:
  truths:
    - "Agent reading recipes_get.md can navigate directly to a recipe by slug without needing a UUID"
    - "Agent reading recipes_edit.md can add multiple ingredients to a section in a single call using the batch endpoint"
  artifacts:
    - path: "skills/recipe-manager/recipes_get.md"
      provides: "Slug lookup documentation"
      contains: "slug"
    - path: "skills/recipe-manager/recipes_edit.md"
      provides: "Batch ingredient add documentation"
      contains: "ingredients/batch"
  key_links:
    - from: "skills/recipe-manager/recipes_get.md"
      to: "skills/recipe-manager/recipes_search.md"
      via: "cross-reference for finding slugs"
      pattern: "recipes_search.md"
    - from: "skills/recipe-manager/recipes_edit.md"
      to: "skills/recipe-manager/recipes_create.md"
      via: "cross-reference for creating sub-resources"
      pattern: "recipes_create.md"
---

<objective>
Update recipes_get.md to document slug-based recipe lookup (GET /api/recipes/:idOrSlug accepts both UUID and human-readable slug). Update recipes_edit.md to document the batch ingredient add endpoint (POST /api/recipes/:id/sections/:sectionId/ingredients/batch).

Purpose: An agent can navigate to a recipe by slug and add multiple ingredients in one call during edit flows.
Output: Updated recipes_get.md and recipes_edit.md with v1.2 API changes.
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update recipes_get.md with slug lookup documentation</name>
  <files>skills/recipe-manager/recipes_get.md</files>
  <read_first>
    - skills/recipe-manager/recipes_get.md (current content to update)
    - skills/recipe-manager/recipes_search.md (to verify cross-reference — search results include slug field)
  </read_first>
  <action>
Update recipes_get.md to document that the `:id` parameter accepts both a UUID and a human-readable slug. Make these specific changes:

**1. Update the "## Endpoint" section.** Change the endpoint display and description:

Replace:
```
GET /api/recipes/:id
```

With:
```
GET /api/recipes/:idOrSlug
```

Update the description paragraph to:

```markdown
Authentication required (see `shared.md`). Returns the full recipe if it belongs to the authenticated user's household. The `:idOrSlug` parameter accepts either a UUID (`a1b2c3d4-e5f6-7890-abcd-ef1234567890`) or a human-readable slug (`tortilla-espanola`).
```

**2. Update the status codes list.** Replace the existing three bullet points with:

```markdown
- `200` — success, returns full recipe detail
- `404` — recipe not found (also returned if the recipe belongs to a different household — no information leak)
```

Remove the `403` line. Slug lookup always returns 404 for cross-household access (both UUID and slug paths). This is a deliberate security choice — no slug-existence leaks.

**3. Add a "## Slug lookup" section** immediately AFTER the "## Endpoint" section and BEFORE the "## Response shape" section:

```markdown
## Slug lookup

The slug is the URL-friendly version of the recipe name, visible in the `slug` field of every recipe response (e.g., `"slug": "tortilla-espanola"`). You can obtain slugs from:

- The `slug` field in search results (`GET /api/recipes` — see `recipes_search.md`)
- The `slug` field in a `RecipeDetailResponse` after creating or fetching a recipe

Example — fetch by slug:

```
GET /api/recipes/tortilla-espanola
```

This returns the same `RecipeDetailResponse` shape as fetching by UUID. If the slug does not exist or belongs to a different household, the response is `404`.

Example — fetch by UUID (unchanged):

```
GET /api/recipes/a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

Both paths return identical response shapes.
```

**4. Update the "## Example" section.** Add a slug example before the existing UUID example. The section should now show:

```markdown
## Example

Request (by slug):
```
GET /api/recipes/tortilla-espanola
```

Request (by UUID):
```
GET /api/recipes/a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

Response:
```
(keep the existing JSON response exactly as-is)
```

**5. Update "## Cross-references"** to add:

Add a bullet: `- To find a recipe `slug`, search via `GET /api/recipes` (see `recipes_search.md`) — each result includes a `slug` field.`

Update the existing "To find a recipe `id`" bullet to: `- To find a recipe `id` or `slug`, search via `GET /api/recipes` (see `recipes_search.md`).`
  </action>
  <verify>
    <automated>grep -q "idOrSlug" skills/recipe-manager/recipes_get.md && grep -q "## Slug lookup" skills/recipe-manager/recipes_get.md && grep -q "tortilla-espanola" skills/recipe-manager/recipes_get.md && grep -c "404" skills/recipe-manager/recipes_get.md | grep -q "[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - recipes_get.md endpoint shows "GET /api/recipes/:idOrSlug" (not just :id)
    - recipes_get.md contains "UUID" and "slug" in the endpoint description paragraph
    - recipes_get.md does NOT contain "403" in the status codes list (only 200 and 404)
    - recipes_get.md contains "## Slug lookup" heading
    - recipes_get.md contains "GET /api/recipes/tortilla-espanola" as a slug example
    - recipes_get.md contains "no information leak" or equivalent explanation for 404-only behavior
    - recipes_get.md Example section shows both slug and UUID request examples
    - recipes_get.md still contains all response shape tables (top-level, sections, ingredients, steps, images)
  </acceptance_criteria>
  <done>recipes_get.md documents slug lookup as first-class alternative to UUID, with example, security explanation, and cross-references to find slugs</done>
</task>

<task type="auto">
  <name>Task 2: Update recipes_edit.md with batch ingredient add endpoint</name>
  <files>skills/recipe-manager/recipes_edit.md</files>
  <read_first>
    - skills/recipe-manager/recipes_edit.md (current content to extend)
    - packages/shared/src/api/foods-units.ts (lines 15-22 — BatchCreateIngredientsRequest exact shape)
  </read_first>
  <action>
Add a new section to recipes_edit.md for the batch ingredient add endpoint. Insert it AFTER the "## Update an ingredient" section and BEFORE the "## Update a step" section.

The new section must contain exactly:

```markdown
## Add multiple ingredients (batch)

### Endpoint

```
POST /api/recipes/:id/sections/:sectionId/ingredients/batch
```

Authentication required (see `shared.md`). Adds multiple ingredients to a section in a single atomic call. If any `foodId` or `unitId` is invalid, the entire batch is rolled back — no partial inserts.

New ingredients are appended after existing ingredients in the section (order values continue from the current maximum).

- `201` — all ingredients created, returns updated section
- `400` — validation error
- `403` — recipe belongs to a different household
- `404` — recipe, section not found, or invalid food/unit ID

### Request body

```json
{
  "ingredients": [
    { "foodId": "f1f2f3f4-...", "unitId": "u1u2u3u4-...", "quantity": 6 },
    { "foodId": "f5f6f7f8-...", "unitId": "u5u6u7u8-...", "quantity": 0.5, "note": "cortadas finas" }
  ]
}
```

Each ingredient item:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| foodId | string (UUID) | Yes | Food ID from `GET /api/foods` |
| unitId | string (UUID) | No | Unit ID from `GET /api/units` |
| quantity | number | No | Amount (can be decimal) |
| note | string | No | Additional note |

### Response

Returns the full `SectionResponse` for the section, including all existing and newly added ingredients with hydrated `foodName` and `unitName`:

```json
{
  "id": "s1s2s3s4-...",
  "title": null,
  "order": 0,
  "ingredients": [
    { "id": "i1i2i3i4-...", "foodId": "f1f2f3f4-...", "foodName": "Huevo", "unitId": "u1u2u3u4-...", "unitName": "unidad", "quantity": 6, "note": null, "order": 0 },
    { "id": "i5i6i7i8-...", "foodId": "f5f6f7f8-...", "foodName": "Patata", "unitId": "u5u6u7u8-...", "unitName": "kg", "quantity": 0.5, "note": "cortadas finas", "order": 1 }
  ]
}
```

### When to use

Use batch add instead of single-ingredient POST when adding 2+ ingredients during an edit flow. For recipe creation, prefer the compound create path in `recipes_create.md` which inlines ingredients in the initial POST.
```

Additionally, update the "## Cross-references" section at the bottom to add:
- `- To add multiple ingredients at once: use batch add above, or compound create in `recipes_create.md`.`
  </action>
  <verify>
    <automated>grep -q "ingredients/batch" skills/recipe-manager/recipes_edit.md && grep -q "Add multiple ingredients" skills/recipe-manager/recipes_edit.md && grep -q "SectionResponse" skills/recipe-manager/recipes_edit.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - recipes_edit.md contains "## Add multiple ingredients (batch)" heading
    - recipes_edit.md contains "POST /api/recipes/:id/sections/:sectionId/ingredients/batch"
    - recipes_edit.md contains "ingredients" array in request body JSON example with foodId/unitId/quantity/note
    - recipes_edit.md contains ingredient item table with foodId (Required: Yes), unitId (Required: No), quantity (Required: No), note (Required: No)
    - recipes_edit.md contains SectionResponse JSON example with hydrated foodName and unitName
    - recipes_edit.md contains "no partial inserts" or "rolled back" explaining atomic behavior
    - recipes_edit.md contains "### When to use" guidance distinguishing batch add from compound create
    - recipes_edit.md Cross-references section mentions batch add and recipes_create.md
    - recipes_edit.md still contains all original sections (Update recipe metadata, Update a section, Update an ingredient, Update a step, Delete operations)
  </acceptance_criteria>
  <done>recipes_edit.md documents batch ingredient add endpoint with full request/response shapes, atomicity guarantees, and usage guidance</done>
</task>

</tasks>

<verification>
- grep "idOrSlug" skills/recipe-manager/recipes_get.md returns at least 1 match
- grep "Slug lookup" skills/recipe-manager/recipes_get.md returns 1 match
- grep "ingredients/batch" skills/recipe-manager/recipes_edit.md returns at least 2 matches
- grep "SectionResponse" skills/recipe-manager/recipes_edit.md returns at least 1 match
- All original sections in both files are preserved (no accidental deletions)
</verification>

<success_criteria>
An agent reading recipes_get.md knows it can use a slug instead of UUID to fetch a recipe. An agent reading recipes_edit.md knows about the batch ingredient add endpoint with its exact request shape, response shape, and atomicity guarantees.
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-bundle-updates/19-02-SUMMARY.md`
</output>

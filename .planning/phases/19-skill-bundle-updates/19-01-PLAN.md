---
phase: 19-skill-bundle-updates
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/recipe-manager/recipes_create.md
  - skills/recipe-manager/shared.md
autonomous: true
requirements: [SKILL-09, SKILL-10]

must_haves:
  truths:
    - "Agent reading recipes_create.md can build a full recipe in 3 calls or fewer using compound create"
    - "Agent reading recipes_create.md or shared.md can resolve a food/unit ID with a single targeted ?name= call"
    - "The old 11+ call sequential path is still documented as a fallback"
  artifacts:
    - path: "skills/recipe-manager/recipes_create.md"
      provides: "Compound create documentation with call-count comparison"
      contains: "Compound create"
    - path: "skills/recipe-manager/shared.md"
      provides: "?name= filter documentation for foods and units"
      contains: "?name="
  key_links:
    - from: "skills/recipe-manager/recipes_create.md"
      to: "skills/recipe-manager/shared.md"
      via: "cross-reference to ?name= filter"
      pattern: "shared.md"
---

<objective>
Update recipes_create.md to document the compound create path (POST /api/recipes with inline ingredients[] + steps[]) and the ?name= filter for targeted food/unit ID resolution. Update shared.md to document the ?name= query parameter on GET /api/foods and GET /api/units.

Purpose: An agent reading these files can create a full recipe in 3 calls (resolve IDs via ?name=, then compound POST) instead of 11+ sequential calls.
Output: Updated recipes_create.md and shared.md with v1.2 API changes.
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared.md with ?name= filter documentation</name>
  <files>skills/recipe-manager/shared.md</files>
  <read_first>
    - skills/recipe-manager/shared.md (current content to preserve and extend)
  </read_first>
  <action>
Add a new section to shared.md AFTER the "## Pagination" section, titled "## Filtering reference lists". This section documents the optional ?name= query parameter available on GET /api/foods and GET /api/units.

The new section must contain:

```markdown
## Filtering reference lists

The food and unit reference endpoints accept an optional `?name=` query parameter for targeted lookups.

| Endpoint | Filter param | Behavior |
|----------|-------------|----------|
| `GET /api/foods?name=tomate` | `name` (optional) | Returns only foods whose name contains "tomate" (case-insensitive substring match) |
| `GET /api/units?name=taza` | `name` (optional) | Returns only units whose name contains "taza" (case-insensitive substring match) |

When the `name` parameter is **omitted or empty**, the full list is returned unchanged (backward-compatible).

Example — resolve the food ID for "Huevo":

```
GET /api/foods?name=huevo
```

Response:

```json
[{ "id": "f1f2f3f4-...", "name": "Huevo" }]
```

Use the returned `id` as `foodId` when creating ingredients. This replaces the need to fetch the entire foods list and scan it client-side.
```

Do NOT modify any existing sections — only append the new section before the end of the file.
  </action>
  <verify>
    <automated>grep -c "?name=" skills/recipe-manager/shared.md | grep -q "[2-9]" && echo "PASS: ?name= documented" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - shared.md contains the heading "## Filtering reference lists"
    - shared.md contains the string "GET /api/foods?name=tomate"
    - shared.md contains the string "GET /api/units?name=taza"
    - shared.md contains "case-insensitive substring match"
    - shared.md contains the example "GET /api/foods?name=huevo"
    - shared.md still contains all original sections (## Base URL, ## Authentication, ## Error codes, ## Error response shape, ## Pagination)
  </acceptance_criteria>
  <done>shared.md documents ?name= filter with endpoint table, behavior description, and concrete example</done>
</task>

<task type="auto">
  <name>Task 2: Update recipes_create.md with compound create and ?name= filter</name>
  <files>skills/recipe-manager/recipes_create.md</files>
  <read_first>
    - skills/recipe-manager/recipes_create.md (current content — will be significantly restructured)
    - skills/recipe-manager/shared.md (after Task 1 — to cross-reference ?name= filter)
    - packages/shared/src/api/recipes.ts (lines 59-79 — exact CreateRecipeRequest shape with optional ingredients/steps)
  </read_first>
  <action>
Rewrite recipes_create.md to document both the new compound create path (primary/recommended) and the old sequential path (fallback). The file must contain these changes:

**1. Update the "## Create recipe" section's "### Request body" table** to add two new optional fields at the end:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| ingredients | array | No | Inline ingredients for the default section (see Compound create below) |
| steps | array | No | Inline instruction steps (see Compound create below) |

**2. Add a new "## Compound create (recommended)" section** AFTER the "## Create recipe" section and BEFORE the "## Resolve food and unit IDs" section. Contents:

```markdown
## Compound create (recommended)

`POST /api/recipes` accepts optional `ingredients[]` and `steps[]` arrays to create the recipe, all ingredients, and all steps in a single atomic call. If any ingredient references an invalid `foodId` or `unitId`, the entire request is rolled back — no orphaned recipe is created.

### ingredients[] item shape

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| foodId | string (UUID) | Yes | Food ID from `GET /api/foods` |
| unitId | string (UUID) | No | Unit ID from `GET /api/units` |
| quantity | number | No | Amount (can be decimal) |
| note | string | No | Additional note (e.g., "finely chopped") |

All ingredients are added to the auto-created default section in array order.

### steps[] item shape

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| body | string | Yes | Instruction text |
| title | string | No | Optional step heading |

Steps are ordered by array position.

### Error handling

- `201` — recipe + all ingredients + all steps created atomically
- `400` with message `"Invalid ingredient data: food or unit not found"` — a `foodId` or `unitId` does not exist; entire request rolled back, no recipe created
```

**3. Update "## Resolve food and unit IDs" section** to document ?name= filter:

Replace the "### List all foods" subsection content. The endpoint call changes from `GET /api/foods` (full list) to showing the targeted filter approach:

```markdown
### Resolve food IDs

```
GET /api/foods?name=huevo
```

Authentication required (see `shared.md`). The optional `?name=` parameter filters by case-insensitive substring match. Omit it to retrieve the full list. See `shared.md` > Filtering reference lists for details.

Response:

```json
[{ "id": "f1f2f3f4-...", "name": "Huevo" }]
```

Use the returned `id` as `foodId` in the `ingredients[]` array.
```

Replace the "### List all units" subsection similarly:

```markdown
### Resolve unit IDs

```
GET /api/units?name=kilo
```

Authentication required (see `shared.md`). Same filtering behavior as foods. See `shared.md` > Filtering reference lists for details.

Response:

```json
[{ "id": "u1u2u3u4-...", "name": "kilogramo", "abbreviation": "kg" }]
```

Use the returned `id` as `unitId` in the `ingredients[]` array (optional).
```

**4. Rewrite "## Recommended sequence"** to show compound create as the primary path:

```markdown
## Recommended sequence

### Compound path (3 calls)

1. **Resolve food IDs** — `GET /api/foods?name=<name>` for each unique food. Cache results across ingredients.
2. **Resolve unit IDs** — `GET /api/units?name=<name>` for each unique unit (skip if no units needed).
3. **Create the recipe with ingredients and steps** — `POST /api/recipes` with `name`, metadata fields, `ingredients[]`, and `steps[]`.

Total: 1 call per unique food + 1 call per unique unit + 1 POST = typically 3 calls for a simple recipe.

### Sequential path (fallback — 11+ calls)

Use this only if you need to add ingredients to multiple named sections (compound create adds all ingredients to the single default section).

1. **Create the recipe** — `POST /api/recipes` with at least `name`. Response includes `id` and `sections[0].id`.
2. **Resolve food and unit IDs** — `GET /api/foods?name=<name>` and `GET /api/units?name=<name>`.
3. **Add ingredients one by one** — `POST /api/recipes/:id/sections/:sectionId/ingredients` per ingredient.
4. **(Optional) Add named sections** — `POST /api/recipes/:id/sections` with a `title`.
5. **Add steps one by one** — `POST /api/recipes/:id/steps` per step.
```

**5. Rewrite "## Example"** to show a compound create example as the primary example:

```markdown
## Example: Compound create

Step 1 — Resolve food IDs:

```
GET /api/foods?name=huevo
```

Response: `[{ "id": "f1f2f3f4-...", "name": "Huevo" }]`

```
GET /api/foods?name=patata
```

Response: `[{ "id": "f5f6f7f8-...", "name": "Patata" }]`

Step 2 — Resolve unit IDs:

```
GET /api/units?name=unidad
```

Response: `[{ "id": "u1u2u3u4-...", "name": "unidad", "abbreviation": "ud" }]`

```
GET /api/units?name=kilo
```

Response: `[{ "id": "u5u6u7u8-...", "name": "kilogramo", "abbreviation": "kg" }]`

Step 3 — Create recipe with everything:

```
POST /api/recipes
```

Request body:

```json
{
  "name": "Tortilla espanola",
  "servingsQty": 4,
  "servingsUnit": "porciones",
  "prepTime": 15,
  "cookTime": 20,
  "totalTime": 35,
  "ingredients": [
    { "foodId": "f1f2f3f4-...", "quantity": 6, "unitId": "u1u2u3u4-..." },
    { "foodId": "f5f6f7f8-...", "quantity": 0.5, "unitId": "u5u6u7u8-...", "note": "cortadas finas" }
  ],
  "steps": [
    { "body": "Batir los huevos con sal." },
    { "body": "Freir las patatas en aceite hasta dorar." }
  ]
}
```

Response (abbreviated):

```json
{
  "id": "r1r2r3r4-...",
  "name": "Tortilla espanola",
  "slug": "tortilla-espanola",
  "sections": [{
    "id": "s1s2s3s4-...",
    "title": null,
    "order": 0,
    "ingredients": [
      { "id": "i1i2i3i4-...", "foodId": "f1f2f3f4-...", "foodName": "Huevo", "unitId": "u1u2u3u4-...", "unitName": "unidad", "quantity": 6, "note": null, "order": 0 },
      { "id": "i5i6i7i8-...", "foodId": "f5f6f7f8-...", "foodName": "Patata", "unitId": "u5u6u7u8-...", "unitName": "kg", "quantity": 0.5, "note": "cortadas finas", "order": 1 }
    ]
  }],
  "steps": [
    { "id": "st1st2st3-...", "title": null, "body": "Batir los huevos con sal.", "order": 0 },
    { "id": "st4st5st6-...", "title": null, "body": "Freir las patatas en aceite hasta dorar.", "order": 1 }
  ],
  "images": []
}
```

Total calls: 5 (2 food lookups + 2 unit lookups + 1 POST). A recipe with 1 food and no units: 2 calls total.
```

**6. Keep all existing sections** that are not being replaced: "## Add a section", "## Add an ingredient to a section", "## Add a step", "## Cross-references". These document the sequential sub-resource endpoints and remain valid for the fallback path.

**7. Update "## Cross-references"** to add a line about shared.md filtering:

Add: `- Food and unit filtering with ?name=: see `shared.md` > Filtering reference lists.`
  </action>
  <verify>
    <automated>grep -c "Compound create" skills/recipe-manager/recipes_create.md | grep -q "[2-9]" && grep -q "ingredients\[\]" skills/recipe-manager/recipes_create.md && grep -q "Sequential path" skills/recipe-manager/recipes_create.md && grep -q "?name=" skills/recipe-manager/recipes_create.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - recipes_create.md contains "## Compound create (recommended)" heading
    - recipes_create.md contains "### ingredients[] item shape" with foodId/unitId/quantity/note table
    - recipes_create.md contains "### steps[] item shape" with body/title table
    - recipes_create.md contains "Invalid ingredient data: food or unit not found" error message
    - recipes_create.md contains "### Compound path (3 calls)" in the Recommended sequence
    - recipes_create.md contains "### Sequential path (fallback" in the Recommended sequence
    - recipes_create.md contains "## Example: Compound create" with full POST body including ingredients[] and steps[]
    - recipes_create.md contains "GET /api/foods?name=" in the Resolve food IDs section
    - recipes_create.md contains "GET /api/units?name=" in the Resolve unit IDs section
    - recipes_create.md still contains "## Add a section", "## Add an ingredient to a section", "## Add a step" sections (sequential path endpoints preserved)
    - recipes_create.md Cross-references section mentions shared.md and Filtering reference lists
  </acceptance_criteria>
  <done>recipes_create.md documents compound create as primary path, ?name= for ID resolution, sequential path as fallback, with full example showing 3-call recipe creation</done>
</task>

</tasks>

<verification>
- grep "Compound create" skills/recipe-manager/recipes_create.md returns at least 2 matches
- grep "?name=" skills/recipe-manager/recipes_create.md returns at least 3 matches
- grep "?name=" skills/recipe-manager/shared.md returns at least 2 matches
- grep "Filtering reference lists" skills/recipe-manager/shared.md returns 1 match
- grep "## Add a section" skills/recipe-manager/recipes_create.md confirms sequential endpoints preserved
</verification>

<success_criteria>
An agent reading recipes_create.md knows to use compound POST /api/recipes with ingredients[] and steps[] as the primary path, can resolve IDs via ?name= filter, and understands the 3-call workflow. The old sequential path is documented as a fallback. shared.md explains ?name= filtering independently.
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-bundle-updates/19-01-SUMMARY.md`
</output>

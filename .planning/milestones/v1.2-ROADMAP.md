# Milestone v1.2: API Ergonomics

**Status:** ✅ SHIPPED 2026-03-21
**Phases:** 15–19
**Total Plans:** 7

## Overview

Make the REST API significantly more ergonomic for agent clients — all changes purely additive, no breaking changes to the UI client. Four user-visible API improvements (compound create, name filters, slug lookup, batch ingredient add) plus a skill-bundle refresh so agents can discover and use every new capability from documentation alone.

## Phases

### Phase 15: Shared Types + Name Filters

**Goal**: The shared package contract is extended for all v1.2 changes, and agents can filter foods and units by name substring in a single request.
**Depends on**: Phase 14
**Requirements**: ERGO-01, ERGO-02
**Plans**: 2 plans

Plans:

- [x] 15-01: Extend packages/shared (FoodItem, UnitItem, BatchCreateIngredientsRequest, CreateRecipeRequest optional arrays) + enable ValidationPipe `transform: true` in main.ts
- [x] 15-02: Add `?name=` filter to FoodsController and UnitsController (conditional Prisma `where`, `@ApiQuery`, integration tests)

**Success Criteria:**

1. `GET /api/foods?name=tomate` returns only foods whose name contains "tomate" (case-insensitive); omitting the param returns the full list unchanged
2. `GET /api/units?name=taza` returns only units whose name contains "taza" (case-insensitive); omitting the param returns the full list unchanged
3. `packages/shared` exports `FoodItem`, `UnitItem`, `BatchCreateIngredientsRequest`, and the extended `CreateRecipeRequest` with optional `ingredients` and `steps` arrays; `yarn build` in `apps/web` succeeds with no type errors
4. Swagger UI shows `?name` as an optional query parameter on both `/api/foods` and `/api/units` endpoints

### Phase 16: Slug/UUID Dual Lookup

**Goal**: Agents and UI clients can navigate directly to a recipe by its human-readable slug without maintaining a UUID cache, and the existing UUID path is unchanged.
**Depends on**: Phase 15
**Requirements**: ERGO-04
**Plans**: 1 plan

Plans:

- [x] 16-01: Add `isUuid` helper + `RecipesService.findByIdOrSlug` (UUID v4 regex, slug branch with `householdId` in `where` predicate, 404-only on miss) + `RecipesController` wiring + integration tests

**Success Criteria:**

1. `GET /api/recipes/tortilla-de-patatas` returns the same `RecipeDetailResponse` shape as `GET /api/recipes/<uuid>` for the same recipe
2. A slug belonging to a different household returns 404 (not 403), preventing slug-existence leaks across households
3. A UUID that does not exist returns 404; a valid UUID continues to work exactly as before
4. Swagger UI documents `:id` as accepting either a UUID or a slug

### Phase 17: Batch Ingredient Add

**Goal**: Agents can add multiple ingredients to a section in a single atomic call, with correct ordering and fully hydrated food/unit names in the response.
**Depends on**: Phase 15
**Requirements**: ERGO-05
**Plans**: 1 plan

Plans:

- [x] 17-01: Define `SECTION_WITH_INGREDIENTS_INCLUDE` constant + `batch-create-ingredient.dto.ts` + `IngredientsService.batchCreate` (`MAX(order)` start, `createMany`, re-fetch) + `IngredientsController` POST batch route + integration tests

**Success Criteria:**

1. `POST /api/recipes/:id/sections/:sectionId/ingredients/batch` with an array of ingredients inserts all items atomically and returns the updated `SectionResponse`
2. New ingredients appended to a non-empty section receive correct `order` values (no collisions with existing ingredients)
3. The returned `SectionResponse` includes fully hydrated `foodName` and `unitName` for every ingredient (no undefined fields)
4. A failed insert (e.g., invalid `foodId`) rolls back all items in the batch — no partial inserts

### Phase 18: Compound Recipe Create

**Goal**: Agents can create a complete recipe with sections, ingredients, and steps in a single API call that is fully atomic — a FK failure rolls back the entire recipe row.
**Depends on**: Phase 17
**Requirements**: ERGO-03
**Plans**: 1 plan

Plans:

- [x] 18-01: Extend `CreateRecipeDto` with `@ValidateNested({ each: true })` arrays + wrap `RecipesService.create` in `prisma.$transaction` + integration tests (success path, FK failure rollback, backward-compat no-arrays path)

**Success Criteria:**

1. `POST /api/recipes` with optional `ingredients[]` and `steps[]` arrays creates the recipe, all ingredients, and all steps in a single transaction and returns a fully hydrated `RecipeDetailResponse`
2. A request with an invalid `foodId` or `unitId` in the ingredients array returns an error and leaves no orphaned recipe row in the database
3. `POST /api/recipes` with no `ingredients` or `steps` arrays behaves identically to the pre-v1.2 create endpoint (no regression)
4. Swagger UI documents the new optional array fields on the `POST /api/recipes` request body

### Phase 19: Skill Bundle Updates

**Goal**: Agent skill files reflect all four v1.2 API changes so an agent reading the updated files can use every new capability without consulting the source code.
**Depends on**: Phase 18
**Requirements**: SKILL-09, SKILL-10, SKILL-11, SKILL-12
**Plans**: 2 plans

Plans:

- [x] 19-01: Update `recipes_create.md` (compound create path, call-count comparison, `?name=` filter for ID resolution) + update `shared.md` (`?name=` filter documentation)
- [x] 19-02: Update `recipes_get.md` (slug navigation example) + update `recipes_edit.md` (batch ingredient add endpoint and pattern)

**Success Criteria:**

1. An agent reading the updated `recipes_create.md` can build a full recipe in 3 calls or fewer using the compound create path (previously required 11+ calls)
2. An agent reading the updated `recipes_get.md` or `recipes_search.md` can navigate directly to a recipe by slug without needing a UUID
3. An agent reading the updated `recipes_edit.md` can add multiple ingredients to a section in a single call using the batch endpoint
4. An agent reading the updated `shared.md` (or `recipes_create.md`) understands the `?name=` filter and can resolve a food or unit ID in one targeted call

---

## Milestone Summary

**Key Decisions:**

- All four API changes purely additive — no schema migrations, no new packages
- Phase 15 first: shared package must compile before compound create (Phase 18) or TypeScript breaks mid-work
- Phase 18 last: `RecipesService.create` is the most critical service method; change only after all patterns are proven in earlier phases
- Slug lookup includes `householdId` in Prisma `where` predicate (never post-fetch check) — null result is always 404, never 403, preventing slug-existence leaks
- Compound create uses `prisma.$transaction` wrapping the full create; `generateUniqueSlug` stays outside transaction (read-only, avoids locking)
- Batch ingredient add computes `MAX(order)` once before batch (not per-item) — prevents order collisions
- `transform: true` on global `ValidationPipe` enabled in Phase 15-01 (not 18) to avoid mid-phase `main.ts` edits
- `isUuid` is a module-level function (not class method) — no DI needed
- `findByIdOrSlug` sets `recipe = null` on cross-household UUID (404); `findAndVerifyOwnership` unchanged for write ops (403)
- `BadRequestException` (not `NotFoundException`) for P2003 in compound create; error message: "Invalid ingredient data: food or unit not found"
- Compound path listed first in Recommended sequence in `recipes_create.md` — unambiguously the primary agent workflow for recipe creation (3 calls)
- `recipes_get.md` uses `:idOrSlug` endpoint display to make slug acceptance unambiguous; 403 removed (slug lookup always 404 for cross-household — no information leak)

**Issues Resolved:**

- Agent recipe-creation call count reduced from 11+ to 3 via compound create
- Slug-based deep links no longer require a UUID cache for agent or UI clients
- Batch ingredient add eliminates N-call per-ingredient pattern for bulk imports
- Skill-bundle docs refreshed so agents can discover all v1.2 capabilities from docs alone

**Issues Deferred:**

- Pending high-priority todo `fix-agent-empty-body-loop.md` (2026-05-15) — agent empty-body validation loop on recipe/meal-plan creation; carried to next milestone

**Technical Debt Incurred:**

- Postgres `ILIKE` is not accent-insensitive for Spanish names (e.g., `huevo` vs `huevó`); spec did not require accent folding — documented as a known limitation
- Mappers (`toIngredientResponse`, `toSectionResponse`) duplicated in `ingredients.service.ts` to avoid cross-service coupling — revisit if a third consumer appears

---

_For current project status, see .planning/ROADMAP.md_

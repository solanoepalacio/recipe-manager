---
phase: quick-260320-h8h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/recipes/DetailTopBar.tsx
  - apps/web/src/app/(app)/recipes/[slug]/page.tsx
  - apps/web/src/components/__tests__/RecipeDetailPage.test.tsx
autonomous: true
requirements: [quick-h8h]

must_haves:
  truths:
    - "Tapping the ellipsis button on a recipe detail page opens a dropdown menu"
    - "The dropdown menu contains an 'Eliminar' option"
    - "Selecting 'Eliminar' shows a confirmation dialog before deleting"
    - "Confirming deletion calls DELETE /api/recipes/:id, shows success toast, and navigates back to /recipes"
    - "Cancelling the confirmation closes the dialog without deleting"
  artifacts:
    - path: "apps/web/src/components/recipes/DetailTopBar.tsx"
      provides: "Dropdown menu triggered by ellipsis button with delete option"
    - path: "apps/web/src/app/(app)/recipes/[slug]/page.tsx"
      provides: "Delete mutation wired to DetailTopBar callback"
  key_links:
    - from: "DetailTopBar.tsx"
      to: "page.tsx"
      via: "onDelete callback prop"
      pattern: "onDelete"
    - from: "page.tsx"
      to: "/api/recipes/:id"
      via: "api.delete mutation"
      pattern: "api\\.delete.*recipes"
---

<objective>
Wire the existing ellipsis button on the recipe detail top bar to a dropdown menu with a delete option. Tapping delete shows a confirmation dialog, then calls DELETE /api/recipes/:id, invalidates recipe list cache, shows a success toast, and navigates to /recipes.

Purpose: Recipes currently cannot be deleted from the UI despite the backend endpoint existing.
Output: Working delete flow from recipe detail view.
</objective>

<execution_context>
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/recipe-manager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/recipes/DetailTopBar.tsx
@apps/web/src/app/(app)/recipes/[slug]/page.tsx
@apps/web/src/components/ui/ConfirmDialog.tsx
@apps/web/src/lib/api-client.ts
@apps/web/src/lib/query-keys.ts
@apps/web/src/components/__tests__/RecipeDetailPage.test.tsx

<interfaces>
<!-- Existing contracts the executor needs -->

From apps/web/src/lib/api-client.ts:
```typescript
export const api = {
  get:    <T>(path: string)                => request<T>('GET', path),
  post:   <T>(path: string, body: unknown) => request<T>('POST', path, body),
  patch:  <T>(path: string, body: unknown) => request<T>('PATCH', path, body),
  put:    <T>(path: string, body: unknown) => request<T>('PUT', path, body),
  delete: <T>(path: string)                => request<T>('DELETE', path),
};
```

From apps/web/src/components/ui/ConfirmDialog.tsx:
```typescript
interface ConfirmDialogProps {
  message: string;
  onConfirm: () => void;
  onCancel: () => void;
  confirmLabel?: string;
  cancelLabel?: string;
  variant?: 'destructive';
}
```

From apps/web/src/lib/query-keys.ts:
```typescript
recipes: {
  all:    ['recipes'] as const,
  list:   (params: Record<string, unknown>) => ['recipes', 'list', params] as const,
  detail: (slug: string) => ['recipes', 'detail', slug] as const,
},
```

Backend endpoint: DELETE /api/recipes/:id — already implemented in recipes.controller.ts, returns { id: string }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dropdown menu and delete callback to DetailTopBar</name>
  <files>apps/web/src/components/recipes/DetailTopBar.tsx</files>
  <action>
Modify DetailTopBar to:

1. Add props: `onDelete?: () => void` and `isDeleting?: boolean` to DetailTopBarProps.

2. Add local state `menuOpen` (boolean, default false). Clicking the ellipsis button toggles `menuOpen`.

3. When `menuOpen` is true, render a small dropdown menu positioned absolutely below the ellipsis button (right-aligned). The dropdown should have:
   - A single item: "Eliminar" with `Trash2` icon from lucide-react, text in `text-destructive`, font-semibold.
   - onClick calls `onDelete` and closes the menu.
   - If `isDeleting` is true, show a Loader2 spinner instead of Trash2 and disable the button.

4. Add a transparent backdrop div (fixed, inset-0, z-40) when menu is open to close the menu on outside click — same pattern used elsewhere in the codebase (e.g., Phase 08-01 backdrop pattern). The dropdown itself should be z-50.

5. The dropdown container: `absolute right-0 top-full mt-1 bg-background border border-border rounded-[8px] shadow-md py-1 min-w-[160px] z-50`.

6. The menu item: `flex items-center gap-2 px-4 py-2.5 text-[15px] text-destructive font-semibold w-full`.

7. Wrap the ellipsis button and dropdown in a `relative` container div.

Keep existing props and behavior unchanged. `onDelete` is optional so the component remains backward-compatible — if not provided, the ellipsis button still renders but the menu only shows if `onDelete` is defined.
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && npx vitest run apps/web/src/components/__tests__/RecipeDetailPage.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Ellipsis button opens a dropdown with "Eliminar" option. Clicking outside closes it. onDelete callback fires when Eliminar is clicked.</done>
</task>

<task type="auto">
  <name>Task 2: Wire delete mutation in RecipeDetailPage and add test</name>
  <files>apps/web/src/app/(app)/recipes/[slug]/page.tsx, apps/web/src/components/__tests__/RecipeDetailPage.test.tsx</files>
  <action>
**In page.tsx:**

1. Add a `deleteMutation` using `useMutation`:
   - `mutationFn`: `() => api.delete<{ id: string }>(\`/recipes/${recipeId}\`)`
   - `onSuccess`: invalidate `queryKeys.recipes.all` (clears list cache), show `toast.success('Receta eliminada')`, navigate to `/recipes` via `router.push('/recipes')`
   - `onError`: `toast.error('No se pudo eliminar la receta. Intenta de nuevo.')`

2. Pass `onDelete={() => deleteMutation.mutate()}` and `isDeleting={deleteMutation.isPending}` to the `<DetailTopBar>` component.

3. However, deletion should require confirmation. Two approaches — use inline ConfirmDialog pattern (consistent with codebase). Add state `showDeleteConfirm` (boolean). When `showDeleteConfirm` is true, render a `<ConfirmDialog>` right below the DetailTopBar with:
   - `message="Seguro que quieres eliminar esta receta? Esta accion no se puede deshacer."`
   - `confirmLabel="Eliminar"`
   - `onConfirm={() => { setShowDeleteConfirm(false); deleteMutation.mutate(); }}`
   - `onCancel={() => setShowDeleteConfirm(false)}`

4. Change DetailTopBar `onDelete` to `() => setShowDeleteConfirm(true)` (opens confirmation, does not delete directly).

5. Import `ConfirmDialog` from `@/components/ui/ConfirmDialog`.

**In RecipeDetailPage.test.tsx:**

Add a new test: "deletes recipe after confirmation". Steps:
- Mock `api.delete` to resolve `{ id: 'uuid-123' }`.
- Render page, wait for recipe to load.
- Click the ellipsis button (aria-label "Mas opciones").
- Click "Eliminar" in the dropdown.
- Assert the ConfirmDialog appears with the confirmation message.
- Click the "Eliminar" confirm button in the dialog.
- Assert `api.delete` was called with `/recipes/uuid-123`.
  </action>
  <verify>
    <automated>cd /home/solanoe/code/recipe-manager && npx vitest run apps/web/src/components/__tests__/RecipeDetailPage.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Clicking ellipsis > Eliminar shows confirmation dialog. Confirming calls DELETE /api/recipes/:id, shows toast, and navigates to /recipes. Test passes.</done>
</task>

</tasks>

<verification>
- Existing RecipeDetailPage tests still pass
- New delete test passes
- TypeScript compilation: `cd apps/web && npx tsc --noEmit`
</verification>

<success_criteria>
- Ellipsis button on recipe detail opens a dropdown menu
- Dropdown has "Eliminar" option styled in destructive red
- Clicking Eliminar shows inline confirmation dialog
- Confirming deletion calls DELETE /api/recipes/:id
- On success: toast "Receta eliminada", navigate to /recipes, recipe list cache invalidated
- On cancel: dialog closes, no deletion
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/quick/260320-h8h-recipes-can-t-be-deleted-currently-when-/260320-h8h-SUMMARY.md`
</output>
